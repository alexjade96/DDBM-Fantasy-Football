% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/season.R
\name{sl_season}
\alias{sl_season}
\title{Assemble one season of a league}
\usage{
sl_season(league_id, season = NULL)
}
\arguments{
\item{league_id}{Head-of-chain league id.}

\item{season}{Optional season string (e.g. \code{"2024"}); default = most recent.}
}
\value{
A \code{sleeper_season} object.
}
\description{
Fetches and reshapes a single season into a \code{sleeper_season} object holding
the frames every metric builds on: \code{team_wk} (one row per team-week with
points, opponent points, result, all-play tallies), \code{pl_wk} (player-week
points + starter flag + position), \code{lineup} (actual vs optimal each week)
and \code{standings}.
}
\details{
Playoff teams with \code{matchup_id = NA} (eliminated/bye) are handled correctly:
the self-join uses \code{na_matches = "never"}, so they are not paired against
each other and receive no phantom win/loss.
}
\seealso{
\code{\link[=sl_standings]{sl_standings()}}, \code{\link[=sl_luck]{sl_luck()}}, \code{\link[=sl_summary_season]{sl_summary_season()}}
}
