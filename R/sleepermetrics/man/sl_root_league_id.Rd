% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/league.R
\name{sl_root_league_id}
\alias{sl_root_league_id}
\title{The origin (oldest) league_id in a league's season chain}
\usage{
sl_root_league_id(league_id)
}
\arguments{
\item{league_id}{Any league id in the chain (usually the current/head one).}
}
\value{
The chain's oldest league_id.
}
\description{
Stable forever, since a chain only ever extends FORWARD as new seasons are
created (the oldest link's own \code{previous_league_id} is null by
definition, and Sleeper never rewrites history). This is the folder key
\verb{season/<league_id>/} bracket configs should use (see
\code{\link[=sl_playoff_configs]{sl_playoff_configs()}}), NOT any individual season's own, season-specific
id -- Sleeper gives every season of a league a DIFFERENT league_id, so
keying by a single season's id would scatter one real league's brackets
across as many folders as it has seasons.
}
