# 2026-08-02 — Draft tab: redraft toggle, pos_steal fixes, gems/busts redesign, copy cleanup

Repo: `DDBM-Fantasy-Football` (branch `main`). Session resumed from
`.claude/handoffs/current.md` (generated end of prior session, 2026-08-02T00:00:00Z),
continuing the same Draft-tab thread as commit `05ab703`.

## Context

Prior session's handoff was reviewed in full before any action (per its own Resume
Prompt), along with `draft.py` and `tab_draft.html` in their entirety. Git state matched
the handoff exactly (same 5 modified files, same last commit, nothing mid-edit), so no
further changes were made this turn beyond re-verifying and shipping that work.

## Work being committed (all done in the prior/handed-off session, verified again here)

1. **Redraft toggle** on the draft board: "Actual" vs "Redraft by true value"
   (`.db-vs-mode`), backed by new `draft.redraft_board()` + `draft._value_ranks()` and
   `value_rank`/`redraft_round` columns. Actual board's highlight repointed from the old
   `pos_steal`-based threshold to `round - redraft_round`.
2. **`pos_steal` cross-position cap fix**: finish rank capped at the position's DRAFTED
   count, not the full real-NFL universe (fixed the Aiyuk/Mixon backwards-severity bug).
3. **Gems/busts rewritten**: gated + ranked on `pos_steal` alone (not blended with
   `pos_adj`, per explicit user direction), gate = a full round's magnitude
   (`>= team_count` / `<= -team_count`), no count cap (`n=None`), display re-sorted to
   draft order after selection. `undrafted_standouts()` mirrors the same formula.
4. Column layout: dropped displayed `Repl PPG` (still computed, feeds Trend sparkline);
   moved `PPG` between `+/-` and `Pos-adj`.
5. Section copy stripped to bare minimum across Draft finds / glossary / Undrafted panel.
6. **`redraft_board()` rewritten** into an actual simulated per-team-capped draft (was:
   flat ranked list dropped into the real pick sequence, which could stack one team with
   4+ defenses). New `_position_share()` helper shared with `_replacement_level`.

Full rationale, rejected alternatives (`pos_grade` blend, percentile-of-universe,
round-1 ban, 15%-of-draft count cap, drafted-pool-only ranking), and open questions
(the `pos_adj`-vs-`pos_steal` value-concept split between the board highlight/redraft
toggle and gems/busts, not yet reconciled) are preserved in
`.claude/handoffs/current.md` — not duplicated here.

## Verification (this turn)

- `pytest -q` (python/): 51/51 passing, matches handoff's last-recorded state.
- `git diff --stat` matches handoff's file list exactly: `draft.py` (+595/-), `app.py`,
  `style.css`, `tab_draft.html`, `test_core.py`.

## Outcome

User asked to "update & commit/push to github". Committed all 5 modified files as a
single commit and pushed to `origin/main`. No Co-Authored-By trailer per standing
instruction.
