% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/playoffs.R
\name{sl_playoff_replay}
\alias{sl_playoff_replay}
\title{Did the wrong team win? Replay each matchup with optimal lineups}
\usage{
sl_playoff_replay(playoffs, seasons, scope = "title")
}
\arguments{
\item{playoffs}{Named list of \code{sleeper_playoff} objects.}

\item{seasons}{Named list of \link{sleeper_season} objects.}

\item{scope}{See \code{\link[=sl_scope]{sl_scope()}}.}
}
\value{
Tibble: one row per matchup with actual and optimal points for both
sides, and \code{flipped}.
}
\description{
Re-scores both sides of every playoff game using the best legal lineup their
roster could have started, and reports where the result would have flipped.
}
\details{
Only works where the season object actually holds that week's rosters. DDBM
2025's final is week 18, past \code{last_scored_leg} (17), so it cannot be
replayed -- those rows come back \code{NA} rather than being quietly dropped.
}
