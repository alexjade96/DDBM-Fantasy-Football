% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/players.R
\name{sl_players}
\alias{sl_players}
\title{Player name / position lookup}
\usage{
sl_players(refresh = FALSE, cache_path = "sleeperPlayerData.rds")
}
\arguments{
\item{refresh}{Force a re-download even if today's cache exists.}

\item{cache_path}{RDS cache location.}
}
\value{
A tibble with \code{player_id}, \code{player_name}, \code{position}, \code{gsis_id}.
}
\description{
Downloads Sleeper's full NFL player database (the largest payload it serves)
and reduces it to a \code{player_id -> name/position/gsis_id} table. The dump is
league-independent, so it is cached on disk (refetched at most once per day)
and memoised for the session.
}
\details{
\code{DEF} "players" are team defenses; their \code{player_name} is set to the team
abbreviation (the Sleeper \code{player_id}).
}
