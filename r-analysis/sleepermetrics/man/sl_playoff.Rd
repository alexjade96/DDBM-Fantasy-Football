% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/playoffs.R
\name{sl_playoff}
\alias{sl_playoff}
\title{Run a manual / custom playoff bracket}
\usage{
sl_playoff(config, rules = NULL, validate = TRUE)
}
\arguments{
\item{config}{A parsed config (see \code{\link[=sl_playoff_config]{sl_playoff_config()}}) or a path to one.}

\item{rules}{Optional scoring rules; defaults to the config's snapshotted
\code{scoring_settings}, else fetched live from the league.}

\item{validate}{Check each submitted lineup against the league's starting
slots and warn on problems.}
}
\value{
A \code{sleeper_playoff} object: \verb{$results} (one row per team per matchup),
\verb{$players} (per-player breakdown), \verb{$champion}, \verb{$config}.
}
\description{
Resolves a bracket config round by round: each matchup's two sides are scored
from the starters submitted to the commissioner, priced under the league's own
scoring chart, and the winner advances into any \code{"W:<matchup_id>"} reference in
a later round. Byes pass a team through unscored.
}
\details{
Because lineups are an input rather than read from Sleeper, this reproduces a
playoff Sleeper itself could not run (custom week range, custom bracket,
lineups the app had locked).
}
\seealso{
\code{\link[=sl_scoring_chart]{sl_scoring_chart()}}, \code{\link[=sl_plot_playoff_bracket]{sl_plot_playoff_bracket()}}
}
