# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

An R analytics project for Sleeper fantasy football, grown from the "DDBM" redraft league. It began as one-off scripts that pull the public Sleeper API and render `DDBM*.png` charts, and now also ships a reusable **`sleepermetrics`** package + Shiny dashboard that generalise the same analytics to *any* league.

Components (roughly oldest to newest; the scripts are the origin, the package is the productized form):
- **`ddbmFF.R`** the **origin source / historical basis**: single-season analytics (one season's matchups/rosters/transactions to ~26 charts in `results/<season>/`). Run interactively in RStudio (`FantasyFootball.Rproj`) or headless via `Rscript`. **Left untouched**; its chart set has been *ported into and improved within* `R/sleepermetrics` + `python/sleepermetrics`. Ported (all parity-verified, mirrored R↔Python): roster/position analytics (`sl_position_scoring`/`sl_roster`/`sl_starter_bench`), weekly table-position bump line (`sl_table_position`), stacked weekly team points (`sl_plot_team_points`), average position-points box (`sl_plot_position_box`), average roster composition (`sl_roster_counts`), and the transaction-based **trade** and **waiver/FA** performance charts (`sl_trade_performance`/`sl_waiver_performance`), the last two backed by a `transactions` layer now assembled into the `season` object. The trade/waiver metrics improve on the origin: "points while rostered" is read straight from `pl_wk` roster membership (join on `player_id` for trades so both teams show; on `player_id`+`roster_id` for waivers) rather than the origin's fragile transaction-stint reconstruction. Only the team×user treemap variant is intentionally not ported (superseded by `sl_position_scoring` + the roster heatmap).
- **`leagueAnalytics.R`**: cross-season *career* analytics. Walks the whole season chain and aggregates by persistent `user_id` (same manager across seasons despite changing display names/rosters), writing `results/league/` (career standings, finish trajectory, points-per-season, manager-player loyalty, plus `league-career-summary.md` + `.csv`). Reuses the hardened `callSleeper`/`ensure_cols`/`buildLeagueChain`/NA-matchup logic from `ddbmFF.R`.
- **`R/sleepermetrics/`**: a proper R package (the general, canonical implementation), living inside the `R/` instance dir to mirror `python/`. Roles are segmented into files: `api.R` (`sleeper_api`), `players.R`, `league.R`, `season.R` (assembles a `sleeper_season` S3 object: `team_wk`/`pl_wk`/`lineup`/`standings`/`user_map`/`transactions`/`accounts`, plus the roster-aware optimal-lineup solver and the transactions unnester `sl_unnest_transactions`), `metrics.R` + `weekly.R` (compute to tibbles), `plots.R` (render to ggplot), `summaries.R` (narrate to markdown), `dashboard.R` (Shiny), `discord.R` (Discord). Public API is prefixed `sl_` (e.g. `sl_season()`, `sl_luck()`, `sl_plot_efficiency()`, `sl_summary_season()`, `sl_dashboard()`). Has roxygen docs, a `NAMESPACE`, and network-free `testthat` tests. Load with `pkgload::load_all("R/sleepermetrics")`.
  - **Discord** (`discord.R` + `inst/discordbot/`): `sl_post_weekly()` posts a weekly recap (highlights + scoreboard + charts) to a channel **webhook** (schedulable, no hosting); `sl_discord_serve()` runs a **plumber** interactions endpoint answering slash commands (`/standings`, `/luck`, `/weekly`, `/career`, …); it verifies Discord's Ed25519 signatures via `sodium` (`sl_discord_verify`) and defers each command, delivering the chart/summary as a follow-up. `sl_discord_register_commands()` registers the commands. Needs `plumber`, `sodium`, `curl` (Suggests); the endpoint needs a public HTTPS URL.
- **`app.R` + `R/ddbmMetrics.R`**: the **prototype** Shiny dashboard the package was generalised from (still runnable). The package's bundled app (`R/sleepermetrics/inst/shinyapp/app.R`, launched via `sl_dashboard()`) supersedes it; both hit the same metrics. The optimal-lineup solver in both reads each league's `roster_positions`, so FLEX/SUPER_FLEX/etc. adapt per league.
- **`python/`**: a **parallel Python port** of the metrics engine + Discord bot (a venv-based instance, verified to produce output identical to the R package). Same module segmentation (`sleepermetrics/{api,players,league,season,metrics,weekly,plots,summaries,discord_bot}.py`), `pandas` frames instead of tibbles, `matplotlib` charts mirroring `theme_sleeper`, and a **discord.py** slash-command bot (gateway, no signature/HTTPS needed, unlike the R plumber endpoint). CLI: `python/bot.py {weekly,serve}`; `pytest` tests in `python/tests/`.
- **`launch.py`** (repo base): the **single access point** that starts the Discord bot for either instance: `python launch.py <serve|weekly|dashboard|report> [--r|--python] [args]`. The
instance is a **flag, defaulting to Python**; `--r` selects the R instance, and the
flag may sit before or after the mode. It shells to `python/bot.py` (via `python/venv`) or `R/run_bot.R` (which `pkgload::load_all`s `R/sleepermetrics`; the `R/` dir also holds the legacy `ddbmMetrics.R` prototype). Per-instance config lives in `python/.env` and `R/.env` (templates: `*.env.example`); both default to the DDBM league. Note Windows is case-insensitive so `R/` == `r/`; the tracked directory is `R/`.
- **`season/` + `scoring.R`/`scoring.py` + `playoffs.R`/`playoffs.py`**: a **custom playoff engine** for leagues whose playoff is run *outside* Sleeper (DDBM 2025: custom week range, and lineups submitted to the commissioner because Sleeper's rosters were locked). Sleeper can only express its own bracket, and for 2025 its stored `winners_bracket` is genuinely incoherent (the team it records as losing rounds 1 *and* 2 is also the recorded `p == 1` champion), so the bracket shape must come from config, not the API. **Points are recomputed from first principles**: each submitted starter is priced from raw weekly stat lines (`/stats/nfl/regular/{season}/{week}`) times the league's **point-calculation chart** (`scoring_settings`): `sl_scoring_chart()`, `sl_score_lineup()`. This is verified exact: it reproduces Sleeper's own player points (175/175 in wk15) and, replaying Sleeper's own bracket, all 12 matchup winners + the champion with zero mismatches. `sl_playoff(config)` resolves a bracket round by round; only **roster inputs** per matchup are required; winners advance via `"W:<matchup_id>"` refs, `bye` passes a team through, multi-week rounds sum, undecided/unsubmitted matchups report `PENDING` (so it is safe to run live), and lineups are slot-checked (`sl_check_lineup()`). The chart is snapshotted into the config so finished brackets stay reproducible. Wherever a chart is *displayed*, it is translated out of Sleeper's stat codes into plain English by `statnames.R`/`statnames.py` (`sl_scoring_readable()` / `sm.scoring_readable()`), grouped (Passing/Rushing/…/Points allowed), each weight phrased as a sentence (`pass_yd: 0.04` becomes "1 point per 25 yards"; a fractional *non-yardage* weight stays per-event, so `pass_sack: -0.5` becomes "-0.5 points", never "-1 point per 2"), reading order taken from the dictionary rather than alphabetically (so points-allowed brackets descend), and unknown keys surfaced in an "Other" group instead of being dropped. The dictionary is parity-exported, so the R and Python vocabularies cannot drift. Charts: `sl_plot_playoff_bracket()`, `sl_plot_playoff_matchup()`; live in the dashboard's **Playoffs** tab (`sl_dashboard(playoffs = "season")`). Note `last_scored_leg` caps the `season` object at wk17, but wk18 stats *are* fetchable; the playoff engine reads stats directly, so it spans weeks the season object cannot. **A round's `starters` are the commissioner's submitted lineups and are not derivable from Sleeper**: rosters were locked, so they do not match any Sleeper matchup and no roster-membership check should be added. The 2025 `R4M1` shipped as a stale copy of `R3M1` (same nine ids bar one) which scored plausibly but wrongly, so each matchup now carries a `_slots` line naming its picks in slot order; nine opaque ids make that failure mode invisible. Corrected against the commissioner's sheet: LuckyHarm 132.40, SimonSmith 124.53 (champion unchanged; margin went from a 50.88 rout to 7.87). **Bracket configs live one level down, under `season/<league_id>/<season>.json`** (a Sleeper league id is unique per season, so this also disambiguates two different leagues sharing a season number); `config_paths()`/`sl_playoff_configs()` walk it, keeping only numeric-named subfolders, so `season/adp/` (below) and `season/fixtures/` are transparently skipped. Both engines resolve the whole `season/` root from one env var, `SLEEPERMETRICS_SEASON_DIR` (default `<repo>/season`). See `season/README.md`.
- **`season/adp/`: the Python Draft tab's ADP cache.** `sleepermetrics.draft.redraft_board_adp()` simulates a redraft ordered by Sleeper's own **ADP** (average draft position) instead of true season value ("what the field at large would have drafted", the counterpart to `redraft_board()`'s "what a draft knowing the results would have looked like"). ADP itself comes from an **undocumented** Sleeper endpoint (`api.sleeper.com/projections/nfl/<season>`, reverse-engineered, not part of the documented v1 API `api.py` otherwise wraps), fetched live and, on success, written to `season/adp/<season>.json` so a later offline run (or a future run after Sleeper changes/removes the endpoint) still has the latest successfully-captured snapshot to fall back to. Deliberately **season-scoped, not league-scoped** like the playoff configs above (Sleeper publishes one ADP set per year for the whole platform), so it lives in its own shared subfolder rather than being duplicated into every league's own files. The ADP *format* (`adp_std`/`adp_half_ppr`/`adp_ppr`/`adp_2qb`) is picked per-league from the league's actual `rec` scoring weight and QB slot count (`_adp_field_for`), not assumed. Python-only for now, same precedent as this module's other newer draft analytics below.
- **`python/ffadp/` + `season/adp/<source>/`: the webapp's pre-season ADP Comparison tab.** A **webapp-only** module deliberately kept **outside** `sleepermetrics` (it does ESPN/Yahoo HTTP and has no `Season` context, so it does not belong in the R-parity-diffed engine; `verify.py` is unaffected). It puts redraft average-draft-position from multiple sources side by side with a consensus (mean rank) and spread (max-min rank), historical years back as far as each source has data, and CSV/Excel export. **Redraft only** (Sleeper also exposes dynasty/rookie/IDP ADP; this uses only `adp_std`/`adp_half_ppr`/`adp_ppr`/`adp_2qb`). Modules: `base.py` (the `AdpProvider` ABC + `AdpRow` dataclass; the ABC docstring **is** the new-source authoring contract), `board.py` (`combine()` merges every provider's rows on a canonical key -> per-source ADP+rank + `consensus`/`spread`; `to_frame()` flattens for export; **`PROVIDERS` is the single registration point**, and `FIRST_SEASON`/`EARLIEST_ANY` are auto-derived from each module's `EARLIEST`), `identity.py` (cross-platform player key: `espn_id`/`yahoo_id` -> Sleeper `player_id`, read straight from the raw `sleeperPlayerData_py.pkl` dump because `sleepermetrics.players()` projects those ids away; normalised name+pos fallback), `cache.py` (per-`(source, season)` JSON under `season/adp/<source>/<year>.json`, or `season/adp/<source>/<variant>/<year>.json` when a source keeps a separate snapshot per scoring format -- FFC does; `load`/`save` take an optional `variant`, `load(force=)` skips mem+disk for the reload path), `sleeper.py` (`SleeperAdp`, `EARLIEST = 2020`; **snapshot-first** off the *shared* `season/adp/<year>.json` -- same file `sleepermetrics.draft` writes -- and only calls `draft._fetch_adp_raw` on a missing snapshot or `reload=True`), `espn.py` + `api.py` (`EspnAdp`, `EARLIEST = 2004` but real data ~2015+; the one ESPN call is `lm-api-reads.fantasy.espn.com/apis/v3/games/ffl/seasons/<y>/players?scoringPeriodId=0&view=kona_player_info`, ~20MB, no cookies/league id, monkeypatchable; snapshot-first via `cache.load`, `_trim` drops the ~170 undrafted sentinel; **2019 and 2025 come back all-sentinel from ESPN** and are simply absent), `ffc.py` + `api.ffc_adp` (`FfcAdp`, label `FFCalc`, `EARLIEST = 2010`; Fantasy Football Calculator's free public REST API `fantasyfootballcalculator.com/api/v1/adp/<slug>?teams=12&year=Y`, no auth, updated daily; **the one non-Sleeper source that genuinely splits by all four scoring formats** -- `std`/`half_ppr`/`ppr`/`2qb` -> FFC's `standard`/`half-ppr`/`ppr`/`2qb`, each a separate snapshot under `season/adp/ffc/<fmt>/<year>.json`; rows carry a clean name+pos+team so `identity.resolve()` keys them by name+pos, no cross-id; `std`/`ppr` have data 2010+, `half-ppr` from 2018, `2qb` from 2014, older years error), `rotowire.py` + `api.rotowire_adp` (**one JSON feed backing two board columns**: `rotowire.com/football/tables/adp.php?pos=ALL&scoring=<PPR|Standard>` returns a table carrying several sites' ADP plus RotoWire's own `average` consensus. `_feed(season, scoring, reload)` fetches + `_trim`s it **once** -- memoised per `season:slug`, snapshotted to `season/adp/rotowire/<slug>/<year>.json` -- and each provider slices its column: `RotowireAdp` (`average`, label "RotoWire", `analyst` group) and `YahooAdp` (`yahooppr` -- **this is how the Yahoo column is populated at all**, since Yahoo's own API is OAuth-only; `apps` group). **Current season only** -- the endpoint ignores a `year` param, so `EARLIEST` is the current NFL season computed at import (Jan-Feb counts as the prior season), and every earlier year tags these two "from `<year>`". IDP / O-line rows dropped; names matched by `identity.resolve()` on first+last+pos, no cross-id. `yahoo.py` is a thin re-export of `rotowire.YahooAdp`. *(MFL, NFFC, FFPC and Underdog columns were removed 2026-09 -- they were contest/aggregate feeds, not the mainstream draft apps or analyst consensus the board is scoped to; `ffadp/mfl.py`, `api.mfl_adp`/`mfl_players`, the RotoWire `Nffc`/`Ffpc`/`Underdog` sub-providers and `season/adp/mfl/` are gone.)*), `fantasypros.py` (**stub, and there is no no-auth path**: the ADP pages server-render only a ~5-row preview blob with integer ranks; the full table is XHR-loaded from `api.fantasypros.com` which 403s without an HOF-subscriber `x-api-key`. RotoWire's `average` column is the public consensus substitute). A source returning `[]` is dropped from `columns` and tagged in `sources` with a `why` (`"from <year>"` / `"no public data"` / `"unavailable"`). **The scoring selector changes the Sleeper, FFC and RotoWire columns**; ESPN and Yahoo each publish one ADP that stands in for whatever mode is asked. No TE-premium option (no source publishes TEP ADP). **Columns are grouped by source type** (`base.GROUPS` / each provider's `group` attr): `apps` label "Draft platforms" (Sleeper, ESPN, Yahoo), `analyst` label "Analysis platforms" (FFCalc, RotoWire, FantasyPros-stub -- compiled / consensus ADP with no first-party draft app). `combine()` orders `columns` by that grouping (apps -> analyst, registry order within a group) and returns a `groups` list (`{key,label,columns}`, non-empty only); `to_frame()` / the CSV+Excel export follow the same order. `_adp_table.html` renders a group-label header row spanning each group and stamps every header + body cell with `data-adp-group` / `data-adp-col`. The **data actions (Refresh / CSV / Excel) and the per-group toggle chips live OUTSIDE the ADP card**, in one `.adp-toolbar` row -- actions left, chips right-justified (`margin-left:auto`); `adp_compare` passes a static `adp_groups` list (from `GROUPS`) so the shell can render the chips before the board streams in. The `_adp_compare.html` script wires the persistent chip bar once and re-applies the toggle state after every `#adp-board` htmx swap (`.adp-col-hidden`). The export is always the full board regardless of the chip state. **Committed snapshots (the fallback):** Sleeper `season/adp/{2020..2026}.json`, ESPN `season/adp/espn/{2015-2018,2020-2024,2026}.json`, FFC `season/adp/ffc/{std,ppr}/{2010..2026}.json` + `half_ppr/{2018..2026}.json` + `2qb/{2014..2026}.json`, RotoWire `season/adp/rotowire/{PPR,Standard}/<current-season>.json`. `?reload=1` (a Refresh button) bypasses snapshots and re-pulls live, rewriting them. **Webapp:** routes near `home()` in `app.py` -- `GET /welcome` (`_landing_start.html`), `GET /adp` (`_adp_compare.html`, shell + controls), `GET /adp/data` (`_adp_table.html`, board; `?reload=1` re-fetches), `GET /adp/export.{csv,xlsx}` (current view as attachment; `_adp_frame_and_name`/`_adp_params` shared). `_ADP_SCORING` = Standard/Half PPR/Full PPR/Superflex, default `"ppr"`. Needs `openpyxl` (in `requirements.txt`, pandas `.to_excel` engine); `Dockerfile` has `COPY python/ffadp ./ffadp` (`season/adp/` ships via the existing `COPY season`). **Landing page restyle (same session):** `home.html` is now a loaded-league layout with nothing loaded -- `<header class="top">` + `.utilbar` lookup, a `<nav class="tabs">` sub-nav (Get started / ADP Comparison, both htmx-swapping `#panel`, `hx-push-url="false"`), `<main id="panel">`. Heading "Fantasy Football 4 Fun", `FastAPI(title=...)` matches. `makeSortable` was extracted from `index.html`'s inline script into `/static/table-sort.js` (`window.SMTableSort`), loaded by both pages. **Testing tab:** `plot_playoff_bracket_accent`/`plot_playoff_bracket_full` (and their `TESTING_CHARTS`/`CHART_META`/`/chart` entries) were removed; no parity-checked metric changed. **Known-broken (concurrent Draft-tab work, not this feature):** the Testing tab currently 500s -- a `tab_testing.html` section references a `/tab/testing/part/sleeperboard` handler that does not exist.
- **`verify.py` + `parity/`**: the cross-language test harness. `python verify.py [league_id]` runs the Python `pytest` suite, the R `testthat` suite, then both exporters (`parity/export_{py,r}.py/R`) which emit an identical canonical metric JSON, and diffs them field-by-field (numbers within tolerance, summary text exact). Exit 0 only if all pass. This is how R↔Python parity is enforced; run it after changing either implementation.

## Running

Two ways to run:
- **RStudio (the author's way):** open `FantasyFootball.Rproj` and source/step through `ddbmFF.R` top to bottom. Most analysis blocks have a trailing `# view(...)` comment naming the frame to inspect with RStudio's `View()`.
- **Headless:** `Rscript ddbmFF.R` from the repo root works end to end (verified). Select a season with the `DDBM_SEASON` env var, e.g. `DDBM_SEASON=2024 Rscript ddbmFF.R`; unset/empty uses the most recent season. In RStudio instead set `targetSeason <- "2024"` in the Config block (`NULL` = current).
- **Dashboard:** `shiny::runApp(".")` (or `Rscript -e 'shiny::runApp(".", port=8100)'`) launches the web app; open the printed `http://127.0.0.1:<port>`. It auto-loads the default DDBM league on startup; type any league ID and click **Load league** to switch. Needs `shiny`, `bslib`, `DT`, `ragg` (and `nflplotR` for the demo flair). Note: `runApp` blocks and reads the source once at startup; after editing `app.R`/`R/ddbmMetrics.R` you must stop and relaunch. Verify metric logic **headless** by sourcing `R/ddbmMetrics.R` and calling the functions directly (no app needed); that's the fast path.

Notes:
- The package install lines at the top of `ddbmFF.R` are commented out; install them once into your R library before first run (notably `tidyverse`, `httr2`, `ggplot2`, `treemapify`, `tidytext`, `patchwork`, `RColorBrewer`, and the `nflverse` family).
- `getSleeperPlayers.ps1` is a standalone PowerShell helper that downloads the full Sleeper player dump to a JSON file; it is not called by the R script and writes to a hardcoded absolute path under `~/Documents/Data/FantasyFootball`.

Each chart block ends in a `ggsave(out("DDBM*.png"), ...)`, where `out()` resolves to `results/<season>/`. Chart PNGs are gitignored build artifacts (regenerate by running the script); only `results/<season>/player-patch-log.md` is tracked.

## Data flow / architecture

`ddbmFF.R` is the whole program. It runs in four stages:

0. **Config (season selection).** Near the top, after the `/state/nfl` fetch, a Config block sets `currentLeagueId` (head of the chain) and `targetSeason`. Sleeper stores each season as a *separate* league object chained backwards by `previous_league_id`, so `buildLeagueChain()` walks that chain into a `season -> {league_id, last_scored_leg}` map. The selected `season` (default = most recent) resolves `leagueId`, `lastWeek` (= that season's `last_scored_leg`, the correct loop cap; see "Things that will bite you"), and `outDir`/`out()` for `results/<season>/` output.

1. **General Sleeper data**: `callSleeper(objectId, endpoint)` wraps `https://api.sleeper.app/v1`. Player metadata is the largest payload, so it is cached to `sleeperPlayerData.rds` and only re-fetched when the file's mtime is not today (see the `fileDate == today` guard). The raw nested player list is flattened into `playerDF`, then into two lookups: `playerMap` (team-filtered, `player_id -> name/position/team`, used where "currently on a team" matters) and **`playerInfo`** (team-INDEPENDENT `player_id -> name/position`, used for all name/position joins so released/team-less players resolve without manual patches).

2. **League-specific data**: keyed off `leagueId` (from the Config block, not a hardcoded literal). Rosters and users are merged into `userMap` (`roster_id -> user_name`). The script then loops `1:lastWeek` over `/matchups/{week}` and `/transactions/{week}` to build the three central frames everything downstream depends on:
   - `matchupResults`: one row per team per week with cumulative W/L, points, and computed league `table_position`.
   - `DDBMRosters`: one row per player per team per week, with `position`, `player_points`, and `is_starter`. This is the workhorse frame.
   - `allTransactionsDF`: adds/drops/trades unnested to one row per player movement.

   After `DDBMRosters` is built, `applyPlayerPatches()` applies any `seasonPatches[[season]]` (an id-keyed `tribble`, normally empty; `playerInfo` resolves almost everything) and a per-season patch log is written to `results/<season>/player-patch-log.md`.

3. **Analysis + charts**: built from the `Filter*` versions of those frames (`FilterDDBMRosters`, `FilterMatchupResults`, etc.). Because the week loops only cover scored weeks (`1:lastWeek`), the `Filter*` frames no longer drop a week; they just set the `position` factor ordering. Derived frames (`*OfTheWeek`, `playerRosterPerformances`, `playerTradePerformances`, `playerWaiverPerformances`, roster/position breakdowns) feed the `ggplot` blocks.

## Things that will bite you

- **Week cap = `last_scored_leg`, not `state.week`.** The week loops are capped by `lastWeek`, taken from the league object's `settings.last_scored_leg` (the last fully-scored week, e.g. 17 for 2025; 16 for 2024). Do **not** swap this for `currentSeason$week` (`/state/nfl`): that value collapses to `0` in the offseason (which breaks `1:lastWeek`) and is phase-dependent. `last_scored_leg` is live-safe and per-season. (Historical note: this replaced an earlier hardcoded `currentWeek`/`latestWeek = 18` plus a `-1` exclusion filter.)
- **Player resolution / patches are id-keyed now.** Name/position come from `playerInfo` (team-independent), so released/team-less players resolve automatically; there are no more fragile `DDBMRosters[<row>, ...]` row-index patches. If a player is *genuinely* missing from the Sleeper player DB, add an id-keyed row to `seasonPatches[[season]]`; the run-generated `results/<season>/player-patch-log.md` lists any unresolved `player_id`s. The old row-index comments (e.g. `row 1872 -> 1672`) are retained in `ddbmFF.R` purely as a historical changelog; they are intentional, not stale; preserve them.
- **Playoff weeks have teams with `matchup_id = NA`** (eliminated/bye). The matchup self-join uses `na_matches = "never"` so those teams are *not* paired against each other; they get a single no-result row, and the cumulative record uses `cumsum(coalesce(result == "W", FALSE))` so a no-game week doesn't poison the standings. Removing this guard reintroduces phantom playoff matchups (it silently corrupted records for eliminated teams in every season, e.g. 2025 rezzu was 5-12, corrected to 3-12).
- **Seasons vary in shape; keep it tolerant.** Across the season chain the league has different team counts (2022/2024 = 6 teams, 2023 = 8, 2025 = 10) and different transaction/user column sets. Drop-selects use `-any_of(...)` and `ensure_cols()` backfills missing metadata/settings columns, so don't reintroduce bare `select(-col)` or `select(where(~ !any(is.na(.))))` for optional fields (the latter dropped `team_name` whenever any user left it blank). The per-team chart loop iterates the season's actual users, so a season simply renders fewer per-team charts.
- **Accounts / avatars (`sl_league_accounts()` / `sm.league_accounts()`).** Per-account identity, team name + icons, lives in the season's `accounts` frame, deliberately **separate from `user_map`**: `user_map` is joined into `team_wk`, so any column added there rides along into every metric. The two Sleeper avatar fields have **different shapes** and are easy to confuse: a user's `avatar` is a bare id needing the CDN prefix (`https://sleepercdn.com/avatars/<id>`), while the league-specific `metadata.avatar` (custom team picture) is *already a full url*; `sl_avatar_url()` handles both, so never hand-build one. Career identity keys on the persistent `user_id` (names change between seasons) and takes the **most recent** season's name/icon. In Python, remember pandas `NaN` is **truthy** in Jinja; convert to `None` before templating or a missing avatar renders `src="nan"` (see the `records()` bullet).
- **Season report (`sl_season_report` / `sm.season_report`, `report.R`/`report.py`).** The **export deliverable** (the slothstats-style "clean report"): one **self-contained HTML file** bundling the narrative, headline tiles, a per-manager table, and ~19-22 charts, each **embedded as a base64 PNG data URI** so there are zero external assets; it emails, drops in a drive, and opens offline. The dashboards are for exploring; this is for keeping. The R and Python reports **share the drill-down / table CSS and the lead-table layout** and stay visually matching there (theme-aware light/dark via `prefers-color-scheme` + `data-theme`); the per-team table values are diffed R↔Python. They **no longer match byte-for-byte**: the Python season report was refreshed against the webapp (2026-08, see the "Python season report tracks the webapp" bullet below) -- merged good/bad insight tiles instead of the flat six, an expanded postseason section -- and R's `report.R` was intentionally left on the older shape (Python-webapp-only, mirror to R only on request). It is **presentation, not parity-gated** (the harness doesn't diff the HTML), but every number in it flows from the parity-checked metrics. Chart embedding is **best-effort**: a panel that errors (or has no data) is skipped, never fatal; R must guard `sl_trade_performance`/`sl_waiver_performance` inside that skip because a *column-less* empty frame (only the test fixture; real data is always typed) errors in the `nrow()` pre-check. R needs `base64enc` (`dataURI()`); portraits still honour `SLEEPERMETRICS_NO_IMAGES`. Access: `python launch.py report [league] [--season Y] [--out f.html] [--all] [--r]` (runners `python/make_report.py`, `R/make_report.R`), the **Season report tab** in the FastAPI app, the **/report** route (the standalone file; `download=1` sends it as an attachment), and a **Download season report** button (`downloadHandler`) in Shiny.
- **Report content is one spec, two renderers (Python).** `report_parts()` returns the report as *data*: headings, tiles, narrative, lead table, and ordered sections where each section carries pre-built `html` (the manager tables), `charts` as `(chart_key, caption)`, or **both**. The week-by-week section is a game log followed by its charts, so neither renderer may treat `html` as excluding `charts` (`_section()` omits its `<h2>` when handed an empty title, which is how the combined section avoids a repeated heading). `season_report()` bakes those keys to base64 PNGs (the standalone file); the **web app's report tab renders the same spec natively**: tables inline, charts as ordinary `/chart/<key>` `<img>`, which is why the tab's charts stream in with the shared skeleton **and follow the dashboard theme** (the old iframe embed baked them light forever). A chart's **kind** decides what it is called with: `_chart_args()` maps `"season"` / `"seasons"` / `"playoff"` / `"season+manager"`, and a kind whose input this report lacks drops the panel rather than erroring. The chart keys in `_CHART_FNS` must stay in step with the app's `/chart` endpoint, and a `"season+manager"` key additionally needs an entry in the app's `MANAGER_CHARTS` **and** `manager` forwarded on the img url (`|urlencode`, since a name with a space or `&` otherwise breaks the query string), or it renders in the standalone file but 404s in the tab. Don't put a `<script>` in the standalone file; a test asserts it stays script-free, so all report interactivity is native `<details>`.
- **Python season report tracks the webapp (2026-08 refresh, `report.py` only -- R's `report.R` deliberately untouched).** The season (non-scoped) report's **headline tiles are the webapp Overview's merged good/bad insight tiles**: `_insight_tiles(s)` is a package-local mirror of `app._overview_insight_rows` (the six opposed metrics -- the table / coaching / luck / points allowed / consistency / schedule -- same wording, same `{label, rows:[{tone,holder,value,detail}]}` shape), and `report_parts()` marks the branch with `tiles_kind = "insight"` vs `"flat"`. The **manager** report keeps flat `(label, value, sub)` tiles (`_manager_tiles`) -- one person has no opposed best/worst pair; its cross-manager view is the per-category rank table. `_render_tiles(tiles, kind)` (standalone file) and `tab_report.html` (native tab) each branch on `tiles_kind`; the tab reuses `style.css`'s shared `.stat .insight-row*` rules, the standalone file carries its own `.tile.insight` copy in `_CSS`. **The postseason section now mirrors the Playoffs tab**: `bracket` + `playoff_players_splice` (the spliced-bar "Best playoff players", `scope="title"`), and -- only when `consolation_bracket(s, p)` actually has teams/games -- `consolation_bracket` + `consolation_players_splice` + `consolation_clutch`. `_chart_args` gained the `"playoff_splice"` / `"consolation_bracket"` / `"consolation"` / `"consolation_clutch"` kinds (they build the same single-season `{season: obj}` sub-dicts / `losers_bracket` input the app's `/chart` route assembles by hand); `report_parts()` gates the three consolation keys out of the section list up front so neither surface renders a blank panel on an all-teams-made-the-bracket season (e.g. DDBM 2022). Section heading "Coaching & scoring" -> "Lineup efficiency & scoring" (the site retired "Coaching"). `_mgr_postseason`'s bye rows read plain "Bye" (was "First-round bye" for every bye round, which showed twice for a double-bye team). **The season (non-scoped) report has NO prose recap** -- `report_parts()` returns `narrative = ""` for it and the `.lead` "What the numbers say" block / `tab_report.html`'s `.card.narrative` are both guarded on it, same move the Overview tab made when the insight tiles superseded its `summary | md` card. The **manager** report keeps its manager-specific `_manager_narrative`. `summaries.summary_season` prose is still parity-checked and was NOT touched; it is just no longer rendered in the season report.
- **The per-manager report sections carry the site's player-row furniture (2026-08, `report.py` only).** Portraits, `POS #rank` badges, the FAAB "Cost" column and the Draft-finds columns were unified into every manager section so a player reads the same as on the Roster / Draft / Playoffs tabs. **Portraits are deduped, not inlined per use**: `_pface`/`_face` register each image once and return a `<span class="pface" data-k="pN">` token; `report_parts()` returns `portrait_style`, one `<style>` block with a single `background-image:url(data:...)` rule per unique face (91 rules backing ~430 uses for a real report, ~7 MB instead of ~41 MB). Both surfaces must emit it -- the standalone file bakes it into `_TEMPLATE`'s `<head>`, `tab_report.html` prints `{{ portrait_style|safe }}` at the top of `.reportpanel` (an inline `<style>` in an htmx fragment is fine); `style.css` has `.reportpanel .pface/.face` rules for the `<span>`+`background-image` form (the site's `_ident.html` uses `<img>`). Portraits fetch via `headshots` (disk-cached, honours `SLEEPERMETRICS_NO_IMAGES` -- so the tests, which set it, emit zero portrait rules and the file still passes the `src="http"` / self-contained assertions). `_insight_tiles`/all sections take a `season_position_ranks(s)` map (`_rank_of`/`_rank_badge`) computed once in `report_parts`' scoped branch. Section-by-section: **Week by week** got a Lineup % column and a Started | Bench drill (the `_lineup_mini` helper, gold `.bench-impact` on a row the optimal lineup would swap -- a started player it drops / a bench player it starts, the same both-sides cue as Roster -> Efficiency). **Where the points came from** + **Season standouts** drill tables gained portrait + `POS #rank` + a PPG column. **Draft class** gained Order (`pos_pick_rank`) + draft +/- (`pos_steal`, tinted) + PPG columns. **Trades & the waiver wire**: the "Via" column is now "Cost" showing the FAAB bid ("$3 FAAB" / "FA"), via a report-local `_waiver_group_bids(s)` (one API call per week, `except: {}` offline -> falls back to "waiver"/"FA" -- same webapp-only precedent as `app._waiver_bids`); mini-tables gained portraits + rank. **Rivalries** opponent cells gained the manager account avatar (`_manager_avatars(s)` off `s.accounts`). **Their postseason** lineup tables + facts gained portraits + rank. `summaries` / parity frames untouched; R's `report.R` still text-only.
- **Per-manager report: fact/table styling pass (2026-08 follow-up, `report.py` + `style.css`).** (1) **Player facts** (`_fact_player`, every `_facts()` block in a manager section): now a two-line record -- `.fpl` identity line (portrait + name + `POS #rank`, badge right after the name like Draft-finds, the bare `.pos` label only as a fallback when there's no rank so the two never double up) then `.fnum` (the figure + any `.fnote`) beneath it, rather than name + pos + pts + badge run together. `_bench_regret` routes through `_fact_player` too so its "Bench call" fact matches. (2) **Draft class** is now a full `_draft_finds.html` clone: built from `draft.drafted_players(s)` (the same frame the Draft-finds "Drafted" panel uses), columns `Pick` (round.pick e.g. "3.02", `data-sort` on absolute `pick_no`) / `Player` (portrait + `POS #rank`) / `Pos order` ("RB #5", not "RB5") / `Pos rank` / `draft +/-` (`_steal_span`, green/red) / `Weeks` / `PPG` / `Total pts` / `Pos-adj` (`_steal_span`) / `Trend` (`_spark_svg` -- `draft._sparkline()` output rendered as inline `<svg class="spark-line">`, ~5 CSS lines ported into `_CSS`); table class `teams draftfinds`. (3) **Season standouts drilldown** week table: retuned `--cols` (`minmax(160px,1.8fr) 60px 72px 96px`), and the per-week rows now cover the whole stint (a `Role` started/bench cell, a `Running` total that only advances on started weeks and shows an en-dash on a benched week) with the best / worst started week carrying the gold `.bench-impact` row highlight. New `_mini_table_rows(cols, rows)` = `_mini_table` where each row is `(cells, row_class)`. (4) **Multi-column drill bodies** (`.dt-tables`: Started | Bench, Got | Gave): tighter `minmax(210px,1fr)` columns with a hairline `border-left` gutter between the two `.dt-block`s, collapsing to a stacked `border-top` under 640px; the stray `.dt-tables{overflow-x:auto}` rule from the first pass was removed.
- **Trade direction is NOT recoverable from `trade_performance`.** That metric joins on `player_id` **alone**, deliberately, so both sides of a deal appear on the league-wide chart. Filtering it by `user_name` therefore returns players a manager traded **away** as well as those they acquired, and a player's pre-trade points make them look like that manager's best *add* (this shipped: Bijan Robinson, traded away in wk12, was reported as LuckyHarm's best trade add). Direction only survives in `s.transactions`; use `_mgr_trade_ids(s, rid, "add"|"drop")`. Second trap in the same place: rank and display **`points`** (this manager's points while rostering the player), never **`total`** (the player's points across *every* roster that held him): `total` both picks the wrong row and overstates the figure, and it was silently corrupting the waiver "best pickup" too, where direction was already fine.
- **Transaction ledgers (`metrics.trade_ledger` / `waiver_ledger`, webapp-only).** The Transactions tab's detail tables, and the shape the per-manager report's "Trades & the waiver wire" section uses. They keep the **deal** intact, grouped by `transaction_id`, one row per team in it, rather than reducing to per-player rows. `net` = what the incoming players scored *for this team* minus what the outgoing ones scored *for whoever received them*, with each player's points attributed to the roster that actually took him (so three-way trades work; the sanity check is that a season's nets sum to zero). Waiver rows are collapsed to unique (manager, player) with `times` for churn; points are already a per-(player, roster) total, so a row per add would repeat the same figure and read as two separate hits. **`trade_deals()`** folds the ledger's per-team rows into one entry per deal for display: each side lists only what that team *received*, so a player is named once under whoever got him instead of appearing on both a "received" and a "gave up" row. A deal under 10 points net is reported as "a wash" rather than crowning anybody on noise.
- **Coaching + roster detail (webapp-only, `metrics.coaching_detail` / `bench_regrets` / `roster_detail` / `roster_weeks` + their `*_standouts`).** The tab drill-downs behind the efficiency and position charts. **`bench_regrets`** is the sharp one: per team-week it finds the benched player who most outscored a **same-position** starter (so the swap was legal at the time) and marks `flipped` when that single change would have turned the loss into a win; every flip is re-derivable from `team_wk` + `pl_wk`, and that is the check to run if the logic is ever touched. It considers only the single best swap per week, not combinations, which is what keeps "this one decision lost the game" defensible. `coaching_detail`'s `eff` must stay equal to the parity-checked `efficiency` metric; cross-check it rather than assuming. `roster_detail`'s `bench_pts` is the cost of the *roster carried*, deliberately distinct from lineup efficiency (the cost of the *decision*).
- **"Did they keep him?" is roster membership at `last_week`, not transaction order.** Used by the report's waiver drill and the draft tab. A cut followed by a re-add is churn, not an exit, so comparing a drop week against an add week is wrong; and an add and a drop in the **same week** cannot be sequenced from week numbers at all (both bit). `pl_wk` membership in the final scored week is ground truth and covers drops, trades and same-week churn alike.
- **Per-manager report scope (`manager=`).** Scopes the whole report to one team: manager tiles, a manager narrative, a standings *neighbourhood* (leader to their cluster to last) + a per-category rank table, then sections that are about **them**: week-by-week game log, position mix, season standouts, draft class, rivalries, splits/awards, their own postseason run and their own career. It deliberately does **not** restate the league-wide charts. A manager the selected season doesn't have falls back to the whole-league report rather than erroring.
- **Drill-downs are per-row, never a "show the whole table" toggle** (`_drill_table`): a grid-aligned `<summary>` row that expands into `_facts()` labelled fields and/or `_mini_table()` mini-tables, never a run-on sentence. Ranks render as `_rank_pill()` badges (top third good / bottom third bad) and a manager who *is* the league best/worst in a category gets their name flagged in that column. Both the standalone `_CSS` and the app's `.reportpanel` block in `style.css` must carry these classes; the markup is shared, so a style added in one place only is half-broken in the other.
- **`optimal_lineup()` (season.py)** returns the best legal lineup's *players* tagged with the slot each fills; `optimal_points()` is now just its sum. It feeds the week drill's "Started vs Optimal" comparison. This is a **parity-sensitive path** (`lineup.optimal` feeding the diffed `efficiency` metric), so any change to the selection logic must keep totals identical; re-check with `verify.py`.
- **Lineup *display* order is separate from lineup *fill* order (`SLOT_ORDER` / `assign_slots()` / `slot_sort_key()`, season.py).** Every roster table in the report reads in scoreboard order: `QB, RB1, RB2, WR1, WR2, TE, FLEX, K, DEF`. That is **not** `POSITIONS` order, and deliberately not `optimal_lineup()`'s fill order either: the solver fills every fixed position (K and DEF included) *before* any flex, which is correct for selection but strands FLEX at the end of the list. `assign_slots()` is the counterpart to the solver: it takes a lineup **as given** and works out which slot each player occupies (fixed positions first, then flex from whoever is left, highest scorer first so RB1 outscores RB2), numbering repeats. Use it for anything displayed: the week drill's Started **and** Optimal tables (re-labelling the solver's own picks so the two align row for row; the player set and total are untouched, so parity is unaffected) and both playoff-round lineups. Sleeper's slot ordering is **not** available to recover from: `assemble_season` stores `starters` as a **set**, discarding it. Anyone who fits no slot is appended, never dropped, and a league with no slot config degrades to plain position labels.
- **Sortable tables + cache-busting (web app).** Any `#panel table` sorts by clicking a column header (`index.html`), keying on the first embedded number when every cell has one (`#3 of 10` becomes 3, `9-8` becomes 9, `+41.4` becomes 41.4) and alphabetically otherwise; the draft board opts out via `nosort` (it's a round × slot grid), and tables containing the neighbourhood's `⋯` gap rows skip sorting automatically. `style.css` is linked with `?v=<mtime>` (`asset_v()`) because a **stale cached stylesheet** silently strips the report's grid layout into run-together text; that symptom bit three times before this fix. **A row can carry MULTIPLE `.detail-row` siblings, not just one** (a per-row drilldown that expands into several sub-rows, e.g. the Draft-finds `×N teams` per-team breakdown, one `.detail-row` per team): `makeSortable` groups a trigger row with EVERY immediately-following `.detail-row` so a re-sort moves the whole group together, keyed on the group's leading row, and `prepFaRows` toggles all of them as one unit. Ported from `weekly_report.html`'s own single-sibling version of this same pattern (which had already drifted ahead of the dashboard's shared copy) and extended to the multi-sibling case; check both copies stay in sync if either is touched again.
- **Composite / behavioural metrics (`sl_allplay` / `sl_power_rank` / `sl_manager_profile`).** Three ffwrapped-style additions computed entirely from frames already assembled, no projections or external data. **All-play** surfaces the schedule-independent standings (`allplay_w`/`allplay_l` were already summed into `standings`); `rank_delta = allplay_rank - final_position`, so **positive means the real schedule flattered the team** (finished better than merit) and negative means it robbed them. **Power rank** is a z-scored blend (points 0.35 / all-play 0.30 / recent-form 0.20 / efficiency 0.15); the composite is a *mean/sd-derived* column, so it is **returned unrounded and rounded only at display/export**, per the parity discipline (rounding it inside the metric reintroduces the `.x5` R↔Python tie). **Manager profile** aggregates `transactions` + `lineup` into moves/trades/drops/`lineup_iq`; its `tally()` helper must **defer column access** (a season with zero moves has a column-less empty transactions frame; touching `tx["type"]` before the length guard is a KeyError in Python / handled by NULL in R). Charts live on the **overview** tab (all-play + power) and **transactions** tab (manager profile) in both dashboards.
- **Manager avatars on the team charts (`headshots.R` / `headshots.py` + the `.sl_row_avatars`/`.sl_scatter_avatars` and `_row_avatars`/`_point_avatars` helpers).** Every chart whose rows/points are *teams* (standings, power rank, all-play, efficiency, luck, manager tendencies, PF-vs-PA) hangs the manager's account avatar beside their name, the same identity the Career tab's Managers panel shows, keyed on `user_name` via the season's `accounts` frame (`avatar_url`, falling back to `team_avatar_url`). Same best-effort contract as the player portraits: no avatar / no network gives a plain text label, misses cached, `SLEEPERMETRICS_NO_IMAGES=1` disables. Rows read **`[icon] name  |  bar`**, icon first, like the Managers panel, via one shared identity-axis helper (`.sl_identity_axis` / `_identity_rows`) used by **both** the team and player charts. It keeps the names as real axis text (so the column stays auto-sized) but **left-aligns** them, then hangs the token immediately left of that column: R offsets by `grid::grobWidth()` of the longest label (resolved at *draw* time, so no guessing) and adds a fixed `plot.margin`, while Python draws once and **measures** the rendered label extents. Traps, all of which bit and are now guarded: (1) the **full `/avatars/<id>` url is served as `application/octet-stream`** (~400KB) which the `image/*` guard rejects; use the **`/avatars/thumbs/<id>`** form (a ~15KB `image/png`), which is what a chart token wants anyway; (2) the width used to step the icon back must match what the axis text ACTUALLY renders at: `theme_sleeper()`'s `%+replace%` swaps `axis.text` out wholesale, dropping `theme_minimal`'s `size = rel(0.8)`, so labels inherit the **13pt base size**, not 10.4, and measuring at 10.4 put the icons on top of the names; (3) in R the effective tick element is **`axis.text.y.left`, not `axis.text.y`**: the complete theme defines the `.left` child, which shadows the parent, so setting `axis.text.y` renders *byte-identically* and the gap for the token never opens (and the decoration must be added **after** `theme_sleeper()`, since a complete theme replaces everything before it); (4) `urls[[name]]` on a named atomic vector **errors** on a missing name instead of returning `NULL`, so a season with no `accounts` blew every team chart up; check `%in% names()` first (same trap as `paths[[key]]` in `sl_apply_playoffs`). Avatars are a pure overlay, so parity is unaffected.
- **Player portraits (`headshots.R` / `headshots.py`).** The player-centric bar charts (playoff players, trade/waiver performance) hang each row's Sleeper headshot just left of x=0, keyed off the `player_id` the metric now carries. Sleeper hosts one photo per id at `https://sleepercdn.com/content/nfl/players/<id>.jpg`; a **team defense has no face**, so a `DEF` (or a short all-letter id like `SEA`) falls back to the team logo at `.../images/team_logos/nfl/<abbr>.png`. Everything here is **best-effort and never blocks rendering**: a fetch that 403s (a player with no photo answers 403 with an HTML page, *not* 404, so insist on an `image/*` content-type, don't trust the status), times out, or hits no network returns `NULL`/`None` and the row keeps its plain text label; misses are cached so a 20-player chart doesn't re-attempt 20 dead downloads per redraw. Downloads cache to `~/.cache/sleepermetrics/headshots` (on Windows resolve `USERPROFILE`, not `~`, which R expands to Documents/; both instances must share one cache). **`SLEEPERMETRICS_NO_IMAGES=1` turns portraits off entirely** and is set by both test suites so the tests stay network-free, which also means charts you render in a shell that has it exported come out portrait-less (that is the flag, not a bug). Sleeper serves a PNG from a `.jpg` url often enough that the loader **sniffs magic bytes** rather than trusting the extension. Charts stay parity-checked because portraits are a pure overlay (no metric numbers change), and the exporters now diff `player_id` too.
- **Missing values must be scrubbed at the template boundary (`records()`, app.py).** Every template guards a missing value with `is not none` or `or "N/A"`, and **pandas NaN defeats both**: it is neither `None` nor falsey, so the guard sails through and the cell renders the literal `nan`, or `+nan` once a format string has been applied. This is not a template bug and cannot be fixed template-side one cell at a time; it bit on the playoff summary's champion, the weekly scoreboard's opponent/result/margin, and the manager avatars, each of which *already had* a correct-looking fallback that could never fire. Hand template rows through **`records(df)`** (`to_dict("records")` + NaN/NaT/`pd.NA` to `None`) rather than calling `to_dict` directly, and the guards mean what they read as. The sweep that finds regressions is rendering every tab × season and grepping the HTML for `>nan<`; do that rather than reasoning about which columns can go null.
- **A metric that can be empty needs *wording*, not just a non-null value.** "No value" usually means something specific, and the honest label is rarely blank: a playoff team with no elimination round is the **Champion** (or *Still alive* in an unresolved bracket), and a team with no opponent that week played **no game** (a bye or an eliminated team in a playoff week) rather than having an unreadable result. `sl_playoff_summary()`/`playoff_summary()` therefore return a never-null `outcome` and the column is headed **Outcome**, not "Eliminated in".
- **Elimination is the last loss in the TITLE bracket, not the last loss overall.** Consolation games are played *after* a team is already out, so ranking by them overstates the run; five teams across 2022-24 were reported a round or two deeper than they actually went (2022 diogenesthecat read "Lost in Round 3" for a consolation game after a Round 2 exit). `playoff_summary` filters to `bracket == "title"` and falls back to any loss only for a team that never appears there.
- **Consolation bracket vs losers bracket (`sm.consolation_bracket()`).**  Two distinct postseason sub-competitions, renamed 2026-08 (was `toilet_bowl()` -> `consolation_bracket()`; the old in-bracket `bracket == "consolation"` tag -> `bracket == "losers"`).  **Consolation bracket** = the teams that **missed the main playoffs entirely**, and the games they played once the postseason started -- ordinary Sleeper matchups that never appear in the bracket config, so reading the config alone reports no consolation bracket for such a season (2025: sparky1335 beat rezzu in wk15, so rezzu finished last).  **Losers bracket** = eliminated **finalists** playing on to settle placement (3rd, 5th, ...); `_tag_bracket()` marks those `bracket == "losers"` and `scope_frame(d, "losers")` filters to them.  The config's losers-bracket placement games are between teams that **made** the playoffs, so the loser of the last one is often a strong team (2022: diogenesthecat, who finished **first** in the regular season) -- deriving last place from them is wrong; that comes from the consolation bracket.  `consolation_bracket()` returns `winner` and `last`: **`winner` is the actual winner of the final consolation game** (2026-08 redefine -- it used to return the LOSER, an old "win the toilet bowl = you're the worst" in-joke; the UI now labels it "Consolation bracket champion").  **`last` ("Biggest loser") is the team FIRST OUT of the consolation field**, not the loser of the final game (that team went deepest on the losers' side, nearer mid-table): it resolves Sleeper's `sleeper_losers_bracket()` and takes the loser of the highest-`p` placement game (`p == 5` -> 5th/6th, whose loser is dead last), falling back to the missed team whose consolation run ended earliest (smallest last-week-played, tie-broken by worse regular-season finish), then to the worst regular-season finish and *labelling* that it did; a season where every team reached the playoffs has no consolation bracket and says so.  Corroboration to re-run if touched: in every season that has one, the first-out team is also the worst regular-season finisher.  Its drill-down is a **matchup** drill: each row is the game, expanding to both **lineups** (from `pl_wk`, ordered through `assign_slots`, since a consolation-bracket game is a plain Sleeper matchup and never touches the bracket engine's `players`); the check that the lineup extraction is right is that the starters sum to the team's score.  Sleeper's own `/losers_bracket` endpoint (`sleeper_losers_bracket()`, `plot_playoff_bracket(variant="consolation")` -- note the internal `variant` string kept its old value) is a THIRD thing: the tree that structures the consolation field for a chart; it is drawn by `plot_consolation_bracket()` when it resolves, else a flat week-column fallback.

- **Bracket chart: nothing may sit between the title and the axes (`plot_playoff_bracket`).** The round names are **top-axis tick labels**, so a subtitle drawn at `y≈1.02` renders straight through them; this shipped overlapping and got worse when a second clause was added. There is no subtitle band on this chart: the champion rides **in the title**, and explanatory text goes to the **bottom** via `fig.text` + `tight_layout(rect=...)` (same rule the matrix charts already follow). A bye/idle node shows the team's real weekly score **in parentheses** rather than a dash (`reference_scores(s)` supplies `{(manager, week): points}` and `_ref_label()` sums a multi-week round) because a dash reads as missing data when the number is sitting in the season. Parenthesised precisely because it decides nothing: the team advanced on the bye, not on those points. Python/webapp only; R's `sl_plot_playoff_bracket` is unchanged.

- **Bracket chart layout: round-fan tree for DEFAULT Sleeper brackets, byte-identical flat layout for custom ones (`plot_playoff_bracket`, 2026-08 redesign).** The redesign is **gated on `placements`** (a default Sleeper bracket carries `p`-tagged placement nodes; a hand-authored config like DDBM 2025 does not).  For a default bracket: pass 1 spaces round `cx`'s games by `PAIR_STEP * TREE_FAN**cx` (`TREE_FAN = 1.55`, capped `4.0*PAIR_STEP`), then the final round's title game is pulled onto its feeder-mean (apex), giving a fanned tree; a custom bracket keeps flat `PAIR_STEP` and renders exactly as before ("leave DDBM 2025 alone").  Card constants moved (`CARD_GAP` 0.50->0.62, `LANE` 0.56->0.78, `_MIN_SPAN` 4.3->5.4) for wider spacing.  **The `result == "W"` card floats to the top of EVERY terminal matchup** (`_terminal_mids = {_final_cfg} | placements`), not just the championship final.  **Finishing-place chips** sit on the upper card's right edge of a terminal matchup (winner takes `place`, loser `place+1`); palette is SATURATED (`_PLACE_CHIP`: 1 gold `#f1c40f` / 2 blue `#4f86c6` / 3 bronze `#c9752e` / 4+ violet `#8f6fc0`), NOT a metal-grey ramp (a "silver" 2nd read as the neutral bye card); chip text is a dark/white LITERAL (card fills are always light, dark-mode rule).  **Placement labels** ("Placement game" / "Nth-place game", from `_place_tag`) are HORIZONTAL, left-aligned to the card's left edge, just above the matchup's top card (a rotated label in the connector gutter was unreadable).  **Loser connectors** (`LOSS_LINE = "#d99b9b"`, reddish dashed, `zorder=0`) draw only when a `result in ("L","T")` team reappears in the very next round.  **Round headers are two-line**: round name + `"Week 15"` / `"Weeks 17-18"` (from `p.results`' `weeks` column, scalar or list).  Higher placements float to the TOP of the vertical ordering (`unit_place_rank`: title path 0, placement games by `p`, untagged consolation 1000), default brackets only.  Python/webapp only.

- **The playoffs `scope` control is analytics-only, and dies quietly on a bracket with no consolation.** "Count which games?" filters `p.results` on the `bracket` column (`title`/`consolation`). It **never filtered the bracket graphic**, which always draws every game. Two traps behind "it doesn't work": (1) on a season whose bracket is entirely title games (2025), `title` and `all` return the **identical frame** and the rendered PNGs differ only by the caption text, while `consolation` returns **zero rows** and both charts fall back to the same no-data placeholder, so the control is hidden when `bracket == "consolation"` matches nothing, rather than offering three options two of which are the same; (2) it **cannot** reach the consolation bracket, whose games are plain Sleeper matchups that never enter the bracket engine and so carry no `bracket` tag at all; that is a separate section, not a fourth scope. **The standalone "Playoff analytics" section is GONE (2026-08 reorg, supersedes the "it belongs beside Playoff analytics" framing this bullet used to end on)**: the scope control plus its two charts moved INSIDE Postseason results, above the standings table. Final section order top to bottom is Insights tiles, Bracket, Postseason results (standings table, then the scope control, then the two charts), the Games log (a lazy-loaded, round-grouped scoreboard-to-lineups drilldown, replacing the old bracket walk/per-matchup chart/postseason-weeks table), then the scoring chart. Bracket sits ABOVE Postseason results, not below. Only **two** charts remain scoped by the control now, not three: `playoff_stats` was dropped from this tab entirely (the function/`SEASON_CHARTS`/`CHART_META` entry and its `/chart/playoff_stats` route are untouched; only `playoff_stats_all` on the Career tab still calls it), and `playoff_players` was replaced on this tab by a new `plot_playoff_players_splice` (see below); the Career tab's `playoff_players_all` still dispatches to the old, unspliced `plot_playoff_players`, so both chart functions stay live, just scoped to different tabs now. The Postseason-results table itself changed shape in the same pass: **Seed** is no longer its own column; it now renders as a `#N` badge next to the team name; and **High**/**Low** were dropped; **PPG** is relabeled **Playoff PPG** for clarity now that it sits above a table whose other rate stats (Margin) are also postseason-only.

- **The Games log's round groups name their matchups by SEED, split across `label` (the bolded title) and `blurb` (bye + pick callouts) (`game_log()`, `playoffs.py`).** `label` is the round's display name plus its games in parens, e.g. `"Round 1 (#5 vs #8, #6 vs #7)"` (each `#N` is that team's final regular-season standing, `season_rank`; an unresolved side reads `TBD` rather than being dropped) -- built by `_round_title`, generalized from the round's own shape rather than its display name, since names vary by league/season (2025: "Round 1 (seeds 5-8)"; 2022: plain "Round 1") the same way the splice chart's round-depth coloring already had to sidestep display names. **`_round_title` strips any existing `"(...)"` suffix off the config's own display name before appending the seeded matchups** (this league's "Round 1 (seeds 5-8)" renders as just `"Round 1 (#5 vs #8, #6 vs #7)"`, not a double-parenthesized "Round 1 (seeds 5-8) (#5 vs #8, ...)"), since that suffix is now redundant with the matchups appended right after it, and with the pick round's own blurb sentence below. Only the TITLE strips it -- `_pick_blurb` still reads the UNSTRIPPED name (passed to both functions from the same `base_name`) for its own "pick" detection. `blurb` (rendered under the heading in `_playoff_round.html`, also carries an `is_final` flag on the group) never restates the matchups already in `label`; it's the bye callout (`_bye_blurb`, e.g. `"Seeds 1-4 on a bye."`; its `_seed_ranges` helper collapses consecutive seeds into a range rather than listing every one, and a bye whose seed can't be resolved is still counted ("N more teams") rather than silently dropped), plus for a "choose-your-opponent" round a pick callout (`_pick_blurb`). **Pick-round detection reads the round's own display name for the substring "pick"** (this league's 2025 custom bracket literally names them "Round 2 (seeds 3-4 pick)"/"Round 3 (seeds 1-2 pick)") rather than inventing a second structured config field -- the config's own `_pick` key (e.g. `"seed 3 SearingShadow picked xPsyD"`) is a human-readable comment only, never read by the engine. Within a detected pick round, `_pick_blurb` derives the chooser per game as the LOWER `season_rank` number (the higher seed) with no dependency on the `_pick` comment, orders games chooser-seed-ascending, and phrases every pick but the last as a real choice ("Seed 3 chooses #8") and the last as its automatic leftover ("leaving seed 4 to take on #7") rather than repeating "chooses" for a seed with nothing left to pick from. The consolation-bracket group's `blurb` is unrelated (no seed-matchup structure there) and keeps its own longer sentence explaining these are games between teams that missed the bracket entirely and naming who it eliminated to last place, mirroring `consolation_bracket()`'s own `last`/`basis` fields. Python/webapp only.

- **The regular season is split from the postseason at season assembly, in BOTH languages.** `sl_season`/`assemble_season` filter `team_wk`/`pl_wk`/`lineup` to weeks `1..playoff_week_start-1`, so every downstream metric (standings, luck, efficiency, all-play, power rank, career) is a *regular-season* metric without any of them special-casing anything. Filtering once, identically, in each language is what keeps parity; the exported values changed (939 → 874 records) and both sides moved together. Where a league runs its playoff outside Sleeper those weeks are phantom matchups nobody played: 2025 LuckyHarm read **16-1** against an actual **13-1**. The corroboration that this is right: `final_position` now reproduces **all eight** of the 2025 bracket's stored seeds, where the all-weeks version matched only four. `team_wk_all`/`pl_wk_all`/`last_week_all` keep every scored week for the postseason features, and **`consolation_bracket`, `reference_scores`, `_lineup_of` and `playoff_replay` must read those**; on the scoped frames they silently find nothing (R's `sl_playoff_replay` returned all-null until it was pointed at `pl_wk_all`). `last_week` is now the last *regular-season* week: it is the default for "the latest week" everywhere, and leaving it at `last_scored_leg` indexed an empty frame. Sleeper's own phase signals (`status`, `playoff_week_start`) ride on the season object as `status`/`playoff_week_start`, with `in_progress`/`reg_weeks` helpers; nothing else in the codebase knew whether a season was finished.
- **Chart form follows the data's job; the Weekly tab is the worked example.** Three bar charts in a row got replaced by three different forms: a **dumbbell** per matchup (bars hide *who played whom*; the line between the dots is the margin), a **PF-vs-PA scatter split by the tie diagonal** ("was I beaten or unlucky?" is a two-number question no single-value chart can answer; below the line you won, colour is all-play merit), and a **bump chart** for the race through week N. Bars stay where they are right: counts from zero, part-to-whole. A **Cleveland dot plot** is the fix wherever a zero-based bar wastes the axis: `efficiency` spans 81.5–88.9, so 91.7% of every bar is dead length and the real structure (a four-team break) is invisible; dots carry no zero-origin implication, so the axis can honestly zoom.
- **Scatter labels need a real collision solver, and it must run AFTER `_finish` (`_place_labels`).** A fixed offset works until two teams land in the same corner. `_place_labels` tries ten candidate offsets per point and keeps the first whose *rendered* box clears every placed label and every marker, densest points first; extents are **measured**, not estimated from string length. It must be called after `_finish`, which runs `tight_layout()`; geometry solved before that is stale by the time it draws. Fixed artists (the "lost"/"won" zone captions) have to be passed in via `avoid=`, or the solver treats their space as free. The check to re-run is rendering every week × season and counting text-vs-text and text-vs-marker overlaps from the real extents: it went 25 → 0 across 56 weeks, and eyeballing one week had missed all of them. `plot_pf_pa` and `plot_manager_profile` used to skip this solver entirely (a fixed `xytext` offset, same bug in miniature; avatars/labels overlapped in a tight cluster on real 2025 DDBM data), fixed by capturing `_point_avatars()`'s own return, calling `_finish` first, then `_place_labels(..., avoid=(*avatars, *corners))`. Fixing `plot_manager_profile` exposed a second, smaller collision (a label landing on the y-axis tick text for a point near the left edge), fixed by padding `xlim`/`ylim` by 15% before `_finish`, the same padding convention `plot_boom_bust` already used. `plot_mgr_sos` had an unrelated but adjacent bug: a fixed `loc="lower right"` `ax.legend()` sat directly on top of whichever manager's own dumbbell landed there (real on 2025 data: LuckyHarm's row); the subtitle already states the same grey/coloured encoding, so the legend was dropped rather than repositioned. Same collision recurred twice more (2026-08): `plot_draft_grades_value`'s rows are sorted by `points` ascending, so its fixed lower-right legend sat on the bottom row's own label every time, and `plot_clutch`'s did the same on its scatter; both were dropped for the identical "subtitle already states the encoding" reason rather than fixed with a new position; this shape (a fixed-corner legend colliding with real data, subtitle-redundant either way) is now an established pattern in this codebase, not a one-off.
- **A week is a set of GAMES, not a list of teams (`metrics.week_matchups`).** The weekly scoreboard groups by matchup and drills into both lineups in slot order, each side carrying optimal/efficiency/bench and the single legal same-position swap that cost most. `flips` (that swap alone turning a loss into a win) **must agree with `bench_regrets.flipped`**; that is the check, and it caught the real bug: `team_wk` carries `points` and `pa` but **no `margin` column**, so reading `r.get("margin")` made every game look undecided and found 0 flips league-wide instead of 24. Derive margin as `points - pa`. Teams with no opponent stay as one-sided rows; dropping them makes the scoreboard disagree with the standings.

- **Position handling.** `sortPosition <- c("QB","RB","WR","TE","K","DEF")` is the canonical ordering used throughout for sorting and factor levels; `"FLEX"` is synthesized in the `*OfTheWeek` blocks (best leftover RB/WR/TE), it is not a real Sleeper position.
- **Dark-mode charts (Python web app).** The FastAPI dashboard's charts are server-rendered matplotlib PNGs, so they can't read the viewer's colour scheme themselves; the *structural* palette (surface, text, grid, spines, marker edges, neutral fills) is a switchable token dict `plots.T`, selected by `plots.set_chart_theme("light"|"dark")` which also syncs matplotlib rcParams for the implicit colours (legend/tick text). The client detects `prefers-color-scheme`, carries it in a hidden `#theme` field pulled into every fetch via `hx-include`, and it rides each `/chart` URL as `&theme=`; the OS toggle reloads the open tab. Semantic hues stay fixed (they read on either ground); only the greys/whites are tokenised. **Two traps:** (1) text that sits on an always-light fill (medal discs, playoff-bracket nodes) must stay a hardcoded dark literal, *not* `T["ink"]`, or it vanishes on a light node in dark mode; (2) `plots.T` and matplotlib's pyplot state are process-global, so every render path in `app.py` (the `/chart` endpoint *and* report generation) sets the theme and draws under `_render_lock`; without it two concurrent requests cross themes (a dark chart saved on a white facecolor). The season report bakes its charts as PNGs so it renders them light explicitly. Don't reintroduce a bare `facecolor="white"` or a hardcoded structural grey; it breaks dark mode silently. R's Shiny dashboard is a separate surface and was intentionally left light.
- **Draft data + webapp-only analytics (Python).** `python/sleepermetrics/draft.py` is the only thing that touches Sleeper's draft endpoints (`/league/{id}/drafts` → `/draft/{id}/picks`); it is fetched **lazily** (only when the Draft tab / a draft chart is drawn), *not* baked into `assemble_season`, and memoised per `league:season`. Pick value/steal/bust grading is POSITION-scoped and has three distinct reads (`steal`/`pos_steal`/`pos_adj`); see the dedicated bullets below, not summarized here since the detail matters. A batch of ffwrapped-style analytics was added **Python-webapp-only** (same precedent as dark mode / loyalty / weekly): `metrics.boom_bust`/`strength_of_schedule`/`schedule_swap`/`head_to_head`/`record_book` + their plots, plus draft value/grades and the Draft tab's HTML board (round × slot, position-coloured). These are **new aggregations layered on the existing frames**; they do **not** touch the parity-checked metric contract, so the exporters don't diff them and `verify.py` stays green; the R package has the *metrics* mirrored (`sl_player_loyalty` etc. already exist) but not these newer ones or their charts; mirror into R + Shiny only on request. Traps that bit: `head_to_head`/`schedule_swap` read `team_wk["opp"]` and a populated `user_map`, which the **test fixture** (`conftest.py`) originally lacked; it now carries both (additively), so don't strip them. Matrix charts (`plot_head_to_head`/`plot_schedule_swap`) put column labels on the **top** axis, so any explanatory subtitle must live at the **bottom** (`fig.text`, with `tight_layout(rect=...)`) or it collides with the rotated labels.
- **Every tab follows the header's season; only Career is cross-season.** The Playoffs and Draft tabs used to carry their own in-tab season rail, so the postseason could sit on 2023 while the rest of the page was on 2025; the header's picker is now the single season control. The playoff analytics (`playoff_stats` / `playoff_players` / `clutch`) aggregate **whatever brackets dict they are handed**, so span is chosen by the caller, not by the function: the bare `/chart/<key>` names pass `{key: p}` (this season), the **`_all` suffixed names** pass the whole dict (the Career tab's all-time section). `plots._po_span()` derives the title/subtitle from that dict so a scoped chart never still reads "All Time". Keep the two spans as distinct chart keys; the endpoint already has a `scope` param meaning *bracket type* (title/consolation), and a second scope-ish param is easy to confuse. Each tab template keeps its own oob `_seasons.html` include: `pick()` silently falls back to the latest season when a requested one doesn't exist, and without it the header would advertise a season the page isn't showing.
- **Draft gems/busts/steals are POSITION-scoped, and `steal`/`pos_steal`/`pos_adj` answer three different questions (`draft.py`).** `pos_pick_rank` (order taken within his own position) vs. `pos_value_rank` (rank within position by points the DRAFTING team banked) gives `steal`: team-realized value, scoped to the drafting roster only, so a player traded away mid-season reads near-zero however well he went on to score elsewhere; that's intentional (`steal` is "was this pick a good DECISION for the team that made it"), not a bug to "fix" with a was-he-dropped flag. `pos_pick_rank` vs. `pos_rank` (true leaguewide position finish, capped at how many players were actually drafted at that position, since an uncapped real-NFL universe made a deep position's bust dwarf an equally-deserved bust at a shallow one) gives `pos_steal`: the TRUE-value equivalent, immune to trades/drops. `pos_adj` is a third, independent read: `total` (true season points) minus **replacement level** at the position (`_replacement_level`, the Nth-ranked real player leaguewide, N sized to the league's own starter slots, not the mean of the top N, which an elite outlier skews upward). **`mixed`** flags `pos_steal`/`pos_adj` disagreeing in sign, a rank-based read and a magnitude-based read of the same replacement comparison telling different stories (good by one, bad by the other); deliberately NOT `steal` vs. `pos_steal` (an earlier version): `steal` isn't even a column shown on the table, and comparing it against `pos_steal` conflated two differently-sized comparison pools (drafted-only vs. the full real-NFL universe), which could flip the sign at IDENTICAL points and falsely flag a plain, undisputed bust (2025's Jerry Jeudy) as "mixed" with no real disagreement behind it.
- **`pos_pick_rank` is a visible column (Order, e.g. "RB5") since users read `pos_steal`/`+/-` against overall pick number by default, not position order.** 2025 real case that prompted this: McCaffrey (pick 1.09) looked like a smaller surprise than Taylor (pick 3.02) by raw draft slot, but McCaffrey was only the 5th RB taken (Barkley/Robinson/Gibbs/Henry went first) while Taylor was the 9th; `+/-` compares THAT position-relative order against position finish, never the overall pick, and was unreadable without the order itself on the row. For an UNDRAFTED player, `pos_pick_rank` used to be ONE SHARED CONSTANT for every player at a position (`drafted_n + 1`, e.g. every undrafted WR read "WR59"), which is fine as a `pos_steal` input but reads as a fabricated tie once shown as a column; fixed by individualizing it WITHIN the undrafted tier by true season `total` (best first, ties broken on `player_id`), still starting one worse than the last real pick. This shifts `pos_steal` for every undrafted player except whichever keeps rank 1 in its group (a real, accepted behavior change, not a side effect to avoid; verified it doesn't touch drafted players' `pos_steal` at all). The individualization logic now lives ONCE, in `_undrafted_pos_pick_ranks(board, ranks)`, called by both `undrafted_standouts()` (the main table) and `_pos_pick_ranks()` (the per-team drilldown); they used to reimplement this fallback independently, which is exactly the "compute a paired/derived figure twice and let them drift" shape that already bit once on `weeks`/`ppg` (see below) and had silently made the drilldown's `pos_steal` disagree with the same player's row on the main table.
- **A player can touch more than one roster: drafted-then-traded and a churned waiver add are the same case (`_attach_team_splits`/`_player_team_splits`).** `draft_board()`'s own `points`/`ppg`/`weeks` are deliberately scoped to the DRAFTING roster only (feeds `steal`, above); `_attach_team_splits()` adds `teams`/`splits` to any player-rows frame from `pl_wk_all` directly, independent of draft status, so "was he on more than one roster" and its per-team drilldown are computed identically for drafted and undrafted alike. Per-team split rows grade `pos_adj`/`pos_steal` on an EXTRAPOLATED full season (that team's own PPG × the season's week count, ranked against every other real player's actual total) rather than real partial-season points read against a full-season baseline, which understated every short stint. **A real stretch of unrostered time also gets its own split row**, `user_name = "FA"`, priced from real per-week stat-line production via `_season_trend` (never zero); this fires even for a single-team player with a genuine FA gap (`teams == 1` case), though only the `×N teams` badge is a visible marker on the parent row for that case; the row is still reachable via the existing generic `.detail-row`-sibling JS. The shared Draft-finds table (`_draft_finds.html`, **five** panels: All players/Drafted/Undrafted/Gems/Busts, one structurally-identical row template each, Gems is the reference copy) shows a `Weeks` column (weeks rostered ANYWHERE, summed across every team, not just the drafting team's own stint, which undercounted a traded player) and a `PPG` column that is now **season-wide** (`total / last_week_all`), not "while rostered": deliberately asymmetric with `Weeks`, which stays "rostered by anyone". The parent row's PPG and Weeks denominators differ on purpose since the per-team/per-FA split rows underneath now carry the "while rostered" reads instead. **`Weeks` and the underlying per-team splits MUST come from the SAME (all-teams) source together, never one all-teams and one drafting-team-only.** `draft_board()` computes `rostered_weeks`/`rostered_ppg` internally (renamed to the outward `weeks`/`ppg` in `draft_extremes()`/`all_players_impact()`/`drafted_players()`, alongside the internal `_team_weeks`/`_team_ppg` that stay reserved for `steal`) precisely because an earlier version paired an all-teams `weeks` with the drafting-team-only `ppg`, which silently made a traded player's rate read LOWER than a same-total never-traded player's (2025: Bijan Robinson, 2 more true points than Jonathan Taylor, showed a lower rate because his post-trade `weeks` was cut off while the row's `Weeks` cell read the full season). Column order in the shared table is Pick, Player, Order, Pos rank, draft +/−, Weeks, PPG, Total pts, Pos-adj, Trend, Manager, Manager last so its badge/chevron doesn't compete with the numeric columns.
- **`all_players_impact()` is now built from two half-functions, each independently reusable (`draft.py`).** `drafted_players(s)`: every drafted pick, `draft_board()` + `_attach_team_splits()`, sorted by `pick_no`, backs the **Drafted** panel. `undrafted_universe(s)`: every UDFA at the position (built on `_full_undrafted_universe()`, below), sorted by `total` descending, backs the **Undrafted** panel. `all_players_impact()` is now just `pd.concat([drafted_players(s)[cols], undrafted_universe(s)[cols]])` sorted by `total` descending, replacing ~40 lines of duplicated logic. **`undrafted_standouts(s, n=25)` is a separate, DELIBERATELY UNTOUCHED function**, still rostered-in-this-league-only, still feeding `draft_standouts()`'s headline "best undrafted find" tile (which wants a real DDBM-roster benefited find, not an obscure full-universe one). Do not confuse it with `undrafted_universe()`, and do not redirect that tile without being asked.
- **Gapless position ranking across the FULL real-NFL universe, not just this league's own roster history (`_full_undrafted_universe`/`_never_played_pos_ranks`, `draft.py`).** `_full_undrafted_universe(s, board, ranks)` sources every id in `metrics.season_position_ranks()` (not just `pl_wk_all`) minus whoever's on `board`, so a gap in the ranked universe (e.g. RB #72 between two DDBM-rostered RBs #71/#73) still appears with `user_name` defaulting to "FA" if never rostered in this league. `_never_played_pos_ranks(pos_by_pid, ranks)` gives a drafted player with ZERO real stat lines a synthetic, UNCAPPED "one worse than the deepest real rank" `pos_rank` (e.g. WR #145 if the worst real WR finished #144), individualized gaplessly across multiple such players; this feeds the DISPLAYED `pos_rank` column only. `pos_steal`'s own internal fallback deliberately stays capped at drafted-count (see the two-caps bullet above) and must not be unified with this uncapped display convention. **`_full_undrafted_universe()`'s `pos_steal` has NO cap at all** (not even the drafted-count cap `draft_board()` applies to drafted rows): an undrafted player's `pos_pick_rank` is already past the drafted count by construction, so applying that same cap to his finish manufactured a huge backwards gap (a last-ranked, zero-production undrafted WR read as the single biggest "steal" in the league before this fix). These are two independent, deliberately-not-merged fallback conventions; see `draft_board()`'s and `_full_undrafted_universe()`'s own inline comments before touching either.
- **Real bug fix in `metrics._position_totals()`, a shared primitive used beyond the Draft tab (also `playoffs.py`).** It previously counted a player as "having played" a week for ANY dict-key presence in Sleeper's weekly stats response, including a metadata-only stub Sleeper emits for a player who was on an NFL roster but never touched the field (`{"gms_active": 1.0, "pos_rank_std": 999.0, ...}`; none of those keys are real scoring keys, and `999` is Sleeper's own "unranked" sentinel). Real 2025 case: Joe Mixon and Brandon Aiyuk both carried this exact stub for all 17 weeks with zero real production ever present, yet ranked RB #159/WR #269 as if they'd had real seasons. Fixed by requiring ≥1 key actually present in the league's `rules` dict before counting a week as played. No R-language parity counterpart exists for this function (Python-webapp-only), so this was not a parity-breaking change.
- **Draft-finds default sort differs by panel, not uniform.** All players / Undrafted / Drafted default to `Total pts` descending (Drafted's own natural axis is draft order, `pick_no`, so it's the one exception among these three: actually sorted by `pick_no` ascending). **Gems/Busts default to `pos_steal` (the `draft +/-` column), descending for Gems, ascending for Busts**, i.e. each panel's own severity ranking, most extreme first, deliberately NOT total points; this overrides the general rule for those two panels only.
- **Undrafted players are eligible for BOTH Gems and Busts**, at the identical `pos_steal >= / <= team_count` threshold drafted players use (`draft_extremes()`). Real 2025 data: dozens of undrafted gems clear the gate, essentially zero undrafted busts; mathematically expected, since the individualized undrafted fallback rank keeps that side's `pos_steal` naturally close to zero.
- **A generic client-side table sort (`makeSortable` in `index.html`) falls back to alphabetical for an ENTIRE column the moment even one cell fails numeric detection.** Any Draft-finds panel whose Pick column can mix a real slot ("1.01") with a UDFA sentinel needs an explicit `data-sort="{{ '%07d'|format(r.pick_no) }}"` override on that `<td>`, required on All players, Gems, and Busts (any panel that can contain a mix of drafted+undrafted rows); Drafted and standalone Undrafted are single-type panels and don't strictly need it. The UDFA `pick_no` sentinel itself is `1_000_000 - round(total*10)` (clamped), which is what encodes "always sorts after every real pick, AND total-descending among UDFA rows" in one numeric key, not a flat constant, which would only *coincidentally* sort correctly via browser sort-stability.
- **Draft metrics price the whole Sleeper-tracked season, postseason weeks included: `pl_wk_all`, not `pl_wk` (`draft.py`).** This league's own regular season ends at `last_week`, but Sleeper rosters (started/benched) are still tracked through `last_week_all` even though the real playoff bracket is scored separately by the custom playoff engine (see the regular/postseason split bullet below). `total`/`pos_rank` already priced every real week through `last_week_all`; reading `points`/`weeks`/`teams` from the season-only `pl_wk` compared a partial-season figure against that full-season one, which understated a player who was rostered the ENTIRE regular season but simply never touched postseason production nobody in this league's season played, a real, verified cause of a false `mixed` tag on a player who was never traded or dropped. `redraft_standings()`/`redraft_week_matchups()` deliberately stay `last_week`/`team_wk`-scoped (they replay the real regular-season schedule; there is no postseason schedule to replay against).
- **Draft-finds section: the "mixed" tag was removed, "Pos order"/"Pos rank" now sort by position-then-rank, and each panel has a position filter (`_draft_finds.html`, `index.html` `prepDraftFilters`, `.dffilter*` CSS).** (1) The `{% if r.mixed %}<span class="tag mixed">` badge and its glossary entry are gone from all 5 panels; `.tag.mixed` CSS removed. `draft.py`'s `mixed` field + `test_mixed_*` stay (parity-adjacent metric, still valid) -- only the UI stopped showing it. (2) Both `data-sort`s used the bare zero-padded rank, so sorting either interleaved every position's #1s, then #2s. New `posrank_sort(pos, rank)` macro builds a fixed 7-char key: 1-digit fantasy-position prefix (QB=1..DEF=6, other=7) + 6-digit rank (`999999` if missing). The sort JS compares `data-sort` as a STRING (not numeric) once it's set, so equal length matters; applied to both columns across all 5 panels + the `.detail-row` cells (16 sites). (3) `pos_filter_bar(rows)` renders the Roster tab's `.posbuild` bar chart (`_roster_by_manager.html`) above each panel: one `.pbrow` per present position -- `.pblabel` colour chip (the `data-filter-pos` control) + `.pbtrack` bar scaled to THAT panel's heaviest position (with the panel-share `%` as the in-bar label at `>=12%` wide) + `.pbval` `"N players"`. No "All" chip: the table shows everyone by default, clicking a position filters, clicking the active chip clears. `prepDraftFilters()` is scoped per `.vs-panel`, hides `tr[data-pos]` mismatches (and collapses every `.detail-row` on any filter change), guarded by `dataset.dfFilterable`, wired to `htmx:afterSwap` (the finds section is always swap-loaded). `.dffilter` has no width cap so the bars line up with the table below.
- **`.scrolltable` bug-fixes, and the scrollbar-vs-header question left unresolved (`style.css`).** The sticky-header table wrapper (Waiver wire / Free-agents / Weekly transactions / Draft finds -- `_tx_waivers.html`, `_fa_standouts.html`, `_week_transactions.html`, `_draft_finds.html`) had a stale `border-collapse: separate; border-spacing: 0` override (added on the long-false belief that `collapse` disables `position: sticky`); `separate` was itself causing an inter-cell bleed-through and is now reverted to plain `collapse`, like `.roster-scroll` / `.draftboard`. The sticky `th` keeps `position: sticky !important` (the `#panel ... th[role="button"] { position: relative }` sort-arrow rule outranks a bare class selector) and its bottom divider is a `::before` pseudo (1px `--line`), NOT `border-bottom` / `box-shadow` -- both are dropped on internal cells of a collapsed-border table by Chrome/Firefox, so a plain header border never painted. `.reportpanel .card.tablewrap.scrolltable { padding: 0 }` was added at matching specificity so the Weekly tab's FA table gets the same flush treatment the Transactions tab's identical table already had. **The vertical scrollbar still runs the full box height beside the header** -- confining it to just the rows needs a body-only-scroll structure (either hardcoded per-table column widths or JS width-sync); a flex-column rewrite and a `scrollbar-gutter: stable` attempt were both tried and backed out. Left as-is, consistent with the app's other scroll tables.
- **FAAB cost replaces the "waiver" wording, webapp-Python-only (`app.py` `_waiver_bids` / `_waiver_group_bids` / `_enrich_waiver_cost` / `_faab_in_standouts`).** `transaction["settings"]["waiver_bid"]` rides on every raw Sleeper waiver claim but `season._unnest_transactions` drops it (mirrors the R frame -- adding a column is a parity change), so it is re-read from a fresh `sm.sleeper_api` transactions pull, memoised in `_waiver_bid_cache` per `league:season` (cleared on the `refresh=1` path), degrading to `{}` off-network -- same precedent as `draft.py` / the Week-0 ADP lookup for tx data the parity frame lacks. `_enrich_waiver_cost(rows, s)` rewrites each `waiver_ledger` / `week_transactions` row's `via` to `"$N FAAB"` (or `"$N FAAB / FA"` for a mixed waiver+FA group of the same player, `"FA"` for a pure free-agent pickup) and adds a numeric `faab` (None when unpriced -- a $0 league or off-network). Called in `_tx_part_waivers` and `_week_extras` (before the manager-scope refilter, so filtered rows keep the fields). The "Via" column on both `_tx_waivers.html` and `_week_transactions.html` is renamed **"Cost"**, cell `data-sort` set to the zero-padded `faab` (`-00001` for FA) so it sorts by cost. `_faab_in_standouts` swaps `" &middot; waiver in wk N"` for the FAAB cost in the "Best pickup" transaction-standouts tile when that pickup was a priced waiver claim (an FA pickup -- DDBM 2025's actual Best pickup, Daniel Jones -- is left untouched). Section headings ("Waiver wire" etc.) and `waiver_ledger` / `s.transactions` are NOT touched; R Shiny still shows "waiver". A `$0` waiver-priority claim renders `"$0 FAAB"` (its literal cost, distinct from `"FA"`).
- **Small label changes (`app.py`, `_week_transactions.html`).** The Weekly "Waivers &amp; free agent pickups" table's "Points this week" column -> **"This Week"**. The **Career** tab was renamed **History** and moved to sit just before Testing (`TABS` in app.py); the internal key stays `"career"` so all routes / `/tab/career/part/*` / `_all` chart keys / `?tab=career` bookmarks are unchanged. The prose card heading inside that tab (`### Career - across all seasons`, from `summaries.summary_career`) is parity-checked and was NOT changed.
- **The redraft simulation report (`redraft_report.html`, `_draft_simulation.html`, `draft.redraft_standings`/`redraft_week_matchups`/`redraft_playoff`) is a self-contained alternate world, not an actual-vs-optimal comparison: its UI deliberately carries no "flipped"/differs-from-reality modifiers.** Both `redraft_board()`'s round x slot grid (`_redraft_board_table.html`/`_redraft_board_adp_table.html`) and the simulation's own game drilldowns (`_week_scoreboard.html`'s `opt_default` branch) are entirely one world simulated against itself; a team's simulated record is against its opponent's simulated score too, never a real result scored against an optimal lineup, so nothing in these views is flagged as differing from what actually happened. Two distinct things share the "green/red beat-or-missed" `.dv-pos`/`.dv-neg` tint (style.css) for genuinely different reasons: the **redraft board's "Orig." label** colors by an exact `pick_no` vs `orig_pick_no` comparison (this simulated pick number vs the player's real draft slot, a raw int added alongside the existing formatted `orig_pick` "3.02" string), a real "did this player rise or fall vs reality" question the board is explicitly built to answer, so it's the one exception to the no-modifiers rule; the **actual Draft tab's own board** (`_draft_board_fit.html`) instead tints by a full-round-or-more threshold vs `redraft_round` (app.py's `dv`). By contrast, a "flipped" chip (sim winner differs from the real winner at that bracket position/matchup) was removed entirely: `redraft_week_matchups()`/`redraft_playoff()` no longer even compute `flipped`/`flips`/`champion_flipped` (not just hidden at the template layer), since nothing else read those fields once the chip was gone. `_week_scoreboard.html`'s `opt_default` branch also drops the sim bench's dead `bench_flag="was_started"` wiring (the sim bench data never actually set that field, so it was already a no-op; a bench player there is simply worse than the picks made, full stop, not a missed call to compare against reality). The sim Weekly panel's `opt_lineup`/`opt_bench` rows now carry `pos_rank` (was missing there; already present on the Playoffs sim panel's rows via `redraft_playoff`); `_lineupmacro.html` renders the "POS #rank" tag automatically once a row carries the field, no macro changes needed. `redraft_playoff`'s `real_winner_of_mid` stays computed (it is genuinely load-bearing, feeding `implicit_winner_of` so a later round's literal team-name reference can resolve who advanced); only the display-only `flipped` boolean derived from it was removed.
- **The drill-down family is global CSS now, not `.reportpanel`-scoped.** `.res`, `.q` and the whole `drilltable` / `dt-*` / `.fact` / `.rankpill` component set were scoped when only the report used them; the Coaching, Roster, Draft and Transactions tabs reuse the same markup, and a scoped rule leaves it silently unstyled there. Only the report's *layout adjacency* rules (`.reportpanel .tablewrap table + .drilltable`, …) stay scoped; those are page layout, not the component. The standalone report's `_CSS` still needs its own copy of anything shared.
- **Panel layout traps, all of which shipped broken at least once.** (1) **A grid column template mixing `fr` with fixed px lets the `fr` column absorb every pixel of slack**: `minmax(130px,1.4fr) 90px 80px …` resolved to a 588px name column beside five ~90px numeric ones bunched at the right edge. Give *every* column an `fr` share. (2) Grid items default to `min-width: auto`, so one long value pushes its column past the `minmax()` minimum and squeezes its neighbours; summary cells carry `min-width: 0` + ellipsis. (3) **An element that draws its own frame must not sit inside `.card`**: `.drilltable` (border + radius) and `.draftboard` (bordered cells) both did, giving a frame inside a frame; use a bare `.tablewrap` for the overflow and let `.tablewrap:not(.card) > …` supply the surface. (4) `.stats`/`.grid`/`.card`/`.tablewrap`/`.deals` all carry **zero margin**, so before `#panel > * + *` existed the only vertical space in a panel came from `h2`'s margin-top and any section without a heading sat flush against the block above it. (5) When racing a rule that carries `#panel`, match its specificity exactly: a (1,2,2) selector loses to the sortable-header rule's (1,2,3) silently.
- **A CSS edit does NOT reach an open page (`X-Asset-V`).** `?v=<mtime>` is only evaluated on a full page load, and this is an htmx app: a tab click swaps `#panel` and never re-reads the `<link>` in `<head>`. A page left open across a stylesheet edit therefore renders new markup unstyled *indefinitely*: grid rows collapse to inline spans separated by a single space, which reads as a spacing bug and is not one. Every response now carries an `X-Asset-V` header and an `htmx:afterRequest` handler re-points the link when it moves. Before debugging any "spacing/layout" report, confirm the browser actually has the current stylesheet.
- **Page navigation is warmed several ways, all best-effort (2026-09).** (1) **htmx `preload` extension** (`index.html`): the `.tabs` nav is `hx-ext="preload"` and every tab button carries `preload="mouseover" preload-images="true"`, so hovering a tab fetches its `/tab/<id>` panel (params inherited from the nav's `hx-include`, so the cached response matches the eventual click) and the chart `<img>` URLs inside it, in the background; the click then swaps from htmx's cache. A hover that never becomes a click costs one warmed request. (2) **Server-side per-league warm-up** (`app.py` `_warm_async` / `_warm_league_season`, `_warmed` guard set + `_warm_lock`): the `boot=1` Overview request (landing page AND a header league/season switch, both routed through `tab("overview", boot=1)`) fires a **daemon thread** that pre-calls the four `metrics._cached`-wrapped functions (`roster_detail` / `roster_weeks` / `roster_honors` / `player_honors`) + `draft.draft_board(s)` so the Roster and Draft tabs hit warm caches. Once per `(league_id, season)`; never cleared (a `refresh=1` clears the underlying caches and re-warming is cheap). **Data only, NOT chart rendering** -- matplotlib is serialised under `plots._render_lock`, so a background render would block the charts the user is actually waiting on. `coaching_detail` / `transaction_standouts` / `draft_extremes` are deliberately NOT warmed (they aren't cached, so a thread call is thrown-away work -- the `preload` hover covers those tabs). (3) **Boot prime of the default league** (`app.py` `_lifespan` -> `_prime_default_league`, 2026-09): a FastAPI `lifespan` (NOT the deprecated `@app.on_event`) starts a daemon thread at process start that runs `_assemble_league(DEFAULT_LEAGUE)` into `_cache` (no-op if already fresh). On a host that spins the container down when idle (Render free), this races the cold-start wake against the first request -- if it finishes first, that visitor gets a cache hit instead of the sequential Sleeper chain walk. Doesn't help a brand-new pasted league. (4) **Stale-while-revalidate on `_cache`** (`app.py`, 2026-09): `league_data()`'s assembly is split into `_assemble_league()`; a **stale** hit (past `TTL`) now returns the old entry immediately and kicks `_revalidate_async()` (one background re-assemble, guarded by `_refreshing` + `_cache_lock` so a request burst kicks ONE refresh, not one per request). Purely request-triggered -- no timer -- so it adds no work while idle. Nobody waits on the chain walk after the first population. All four are wrapped/silent: a failure just means that surface pays its own cost later. Python-webapp-only.
- **htmx + its `preload` extension are VENDORED into `webapp/static/` (2026-09), not pulled from unpkg.** `webapp/static/htmx-1.9.12.min.js` + `webapp/static/htmx-ext-preload-1.9.12.js`, referenced by `index.html` (both) and `home.html` (htmx only) as `/static/htmx-*.js` with **no `?v=`** -- the version is pinned in the filename, so a bump means a new file, not a cache-bust. Removes a third-party origin from the cold-open critical path (no extra DNS/TLS/fetch to unpkg) and an unpkg-outage failure mode. They ship in the Docker image via the existing `COPY python/webapp ./webapp`. To upgrade htmx: download the new `htmx.min.js` + `dist/ext/preload.js` for that version into `webapp/static/` under `htmx-<ver>.min.js` / `htmx-ext-preload-<ver>.js`, update the three `<script src>` refs, delete the old files. `builder.js` / `style.css` stay local-with-`?v=` as before (they change often; htmx doesn't). `<head>` also carries `preconnect` / `dns-prefetch` hints for `api.sleeper.app` + `sleepercdn.com` so the panel/chart fetches' TLS handshakes start during shell parse.
- **The header subtitle carries the loaded league id (2026-09).** `index.html`'s `<p class="sub">` under the `<h1 id="league-name">` reads `League Analytics | {{ league }}` (was "Sleeper league analytics · metrics computed live"), with `id="league-sub"`. `_shell_sync.html` emits a matching `<p class="sub" id="league-sub" hx-swap-oob="true">League Analytics | {{ league }}</p>`, guarded on `league is defined and league` (same guard the `#league` input swap uses), so the subtitle follows a league switch / forward-resolved id the same way the `<h1>` and the ID box do. On the instant shell paint it shows the submitted id; the `boot=1` OOB sync then corrects it to the resolved current-season id. Browser `<title>` unchanged.
- **Tab section order: headline numbers → charts → detail tables.** Playoffs, Weekly and Report already read that way; Coaching, Roster, Career and Transactions used to bury their tiles (or, on Draft, the gems/busts verdict) below a wall of four or five charts. Keep new sections in that order. Also: `tab_charts.html` is now **Career-only** (`tab_career.html` renders it); Overview has its own dedicated `tab_overview.html`.
- **There is ONE Overview renderer: `tab_overview.html` via `tab("overview")` (2026-08).** Two paths load a league by id and both must land on the same Overview: the landing page (`/dashboard` -> instant shell -> panel `hx-get="/tab/overview?boot=1"`) and the in-dashboard header form (`index.html`'s `#lookup`, `hx-get="/load"`). `/load` used to render its own `load.html` (a `summary_season()` prose card + `tab_charts.html`'s standings/power_rank/allplay/luck) which drifted far behind the real Overview (no week lead, no merged insight tiles, no phase-aware playoff/consolation tiles), so switching leagues from the header showed a stale page. `load()` now just validates the id (keeping its friendlier "check the id..." error) and **delegates to `tab("overview", ..., boot=1)`**; `_pushed()`'s `boot` branch prepends the same `_shell_sync.html` OOB (title + season picker) `/load` did before. `load.html` was deleted. If you add a third way in, delegate it too rather than hand-building an Overview ctx.
- **Overview "Regular season" takeaways are MERGED good/bad tiles (`_overview_insight_rows` in app.py + `tab_overview.html`, 2026-08).** Every opposed metric (coaching / luck / points allowed / consistency / schedule, plus "The table" = leader + last) comes back as ONE tile `{label, rows: [{tone, holder, value, detail}, ...]}` whose `rows` stack a green ▲ good row and a red ▼ bad row (`.insight-rows` / `.insight-row` in style.css), not two separate tiles or a spread in the value.  A flat single tile (`{label, value, holder, detail, tone}`) is still a valid shape.  "The table" leads; coaching sorts before luck.  Weekly-high-score-crowns and biggest-mover tiles were dropped.  The CHAMPION is NOT here -- it belongs to the season-complete lead tiles (below).
- **The Weekly tab shows six per-week merged tiles under an "Week N insights" heading, same shape/markup as the Overview's (`_week_insight_rows` in app.py + `tab_weekly.html`, 2026-08).** Replaced the old four single-value KPI tiles (Highest score / Biggest blowout / Closest game / Most points benched). `_week_insight_rows(s, wk)` returns the identical `{label, rows: [{tone, holder, value, detail}]}` shape `_overview_insight_rows` uses, rendered by a copy of the same `.stat` + `.insight-rows` block. The labels, in order, are **Scoring / Efficiency / Bench / Luck / Opponent / Margin** -- per-week analogues of the season six (season "Coaching" -> "Efficiency" here; the two lineup-quality reads, Efficiency + Bench, sit together right after Scoring). The two season-only concepts (Consistency = week-to-week SD, Schedule = opponents' season average) have no single-week meaning and are swapped for Margin (blowout/closest winner) and Bench ("points on the bench, fewest/most"). All from `metrics.week_stats(s, wk)`. Empty on a week with no scored games. `_week_context`'s `kpi_top`/`kpi_blow`/`kpi_close`/`kpi_bench` still exist -- the standalone `/report/weekly` (`weekly_report.html`) and the Overview lead's regular-phase `else` branch still use them.
- **"Un-optimal result" is a Weekly RECAP chip, not a top-level insight tile (`_both_optimal_flips` -> `_week_recap`'s chip list, app.py).** A matchup whose winner would CHANGE if BOTH teams had started their best legal lineup: `loser optimal > winner optimal` while `loser actual < winner actual` (so on a best-lineup value basis the game should have gone the loser's way -- the result that stood is the "un-optimal" one). Deliberately NOT "my optimal beats the opponent's ACTUAL score" -- the opponent gets their optimal too; this is the whole-lineup, both-sides counterpart of `bench_regrets.flipped` (one legal same-position swap, one side). `_both_optimal_flips(s, wk)` pairs sides via `s.team_wk`'s `opp` roster-id joined to `s.lineup`'s `optimal` and returns them biggest-margin first; `_week_recap` adds the top one as a chip labelled "Un-optimal result" (chip cap bumped 3 -> 4 to fit it). The standalone `/report/weekly` shares `_week_recap`, so it gets the chip too.
- **The Weekly TAB's recap is line-items, no lead prose; the standalone report keeps both (`weekly_tab` flag in `_week_narrative.html`).** `tab("weekly")` sets `ctx["weekly_tab"] = True`; the shared `_week_narrative.html` then drops `recap.lead` (the top-score / toughest-week prose is already said by the "Week N insights" tiles right above it on the tab) and renders `recap.chips` as a plain `<ul class="recap-lines">` line list (`.reportpanel .narrative ul.recap-lines` in style.css overrides the wrapping chip-card row the standalone report still uses; each `<li>` is `white-space: nowrap` + ellipsis, and `_week_recap` writes every chip `value` short -- `#10 → #3`, `won outscoring only 3 of 9` -- to fit one line). Headings: **"Week N insights"** above the six merged tiles, **"Week N recap"** above the narrative -- both live in `tab_weekly.html`, NOT the shared `_week_narrative.html`, so the standalone report frames its own copy. An empty chip list on the tab shows "Nothing out of the ordinary this week."
- **Overview complete-phase lead swaps blowout/closest for consolation standouts.** When `s.status == "complete"`, the lead tiles are Champion / Highest score / **Consolation Bracket Winner** (`consolation.winner`) / **Biggest Loser** (`consolation.last`), via `_playoff_tiles(..., tail=, include_margins=False)` -- `include_margins=False` drops Biggest blowout + Closest game.  Highest score / Biggest blowout tiles now name the round the record fell in (`_playoff_tiles(..., game_round={id(game): "Round N"})`, built by `_rounds_of()` which trims the "(#5 vs #8, ...)" suffix off the round label).  Tile dicts are `{value, user_name, detail}`.  The two consolation tiles carry the **same `detail` subtext the Playoffs tab's consolation tiles show** (`tab_playoffs.html`), branched on `consolation.basis == "game"`: Winner -> "won the consolation bracket" / "best missed-bracket finish", Biggest Loser -> "first loser in the consolation bracket" / "worst regular-season finish".  Built in the `tab("overview")` `_cons_tail` list; the `overview_tiles` template loop already renders `kpi.detail` after a `·`.
- **Overview lead phase (`_season_phase`) intentionally does NOT flip to "playoffs" the moment the regular season ends.** The lead is: `s.status == "complete"` -> `phase = "complete"` (button "Playoffs" -> Playoffs tab); else a **title round with a decided game** (`played_rounds`, a bye alone doesn't count) -> `phase = "playoffs"` (button `Playoff <round>` -> Playoffs tab, leads with the last *completed* round, never a PENDING-only future one); else -> `phase = "regular"` (button "Latest week: Week X" -> Weekly tab). So while the regular season is over but round 1 is still being played, the lead stays on the last completed regular-season week -- this is deliberate (the user-facing contract is "last completed week analysis"), only flipping to playoffs once a round finishes. Verified 2026-08; no change made.
- **The pre-week-1 (drafted-but-not-started) season degrades every performance surface to a one-line note (2026-09, `_season_phase` "preseason" phase + `_base_ctx` `scored` gate).** Sleeper flips a season to `status=in_season` a little before week 1 locks, so `season.py` forces `Season.last_week >= 1` and `team_wk` for week 1 exists with every `points` at `0.0` -- "is the season live" is therefore NOT "any metric has data". Three webapp helpers next to each other in `app.py`: `_last_completed_week(s)` (highest `team_wk` week with a positive score, else 0), `_has_scored_data(s)` (that `> 0`), `_week_scored(s, wk)` (`bool()`-wrapped, since `.any()` is `np.bool_`). `_season_phase` gained a 4th phase, `"preseason"` (drafted -- `draft.draft_board(s)` non-empty -- and `_last_completed_week == 0`); the Overview branch checks it FIRST and returns early with ONLY the drafted-rosters table (`_preseason_rosters`, reused from Weekly Week 0) + a "Draft Board ->" button + the lazy config section. `regular`/`playoffs`/`complete` carry `last_week` = last completed week, and `tab("overview")` opens the lead there with `ctx["live"] = False`. `_base_ctx` exposes `"scored" = _has_scored_data(s)` to EVERY tab and lazy part; `report.py`'s package-local mirror is `_has_scored_week(s)` (reads `s.team_wk` directly). Gated on `not scored` with a short "fills in once week 1 is complete" note in place of a chart wall: `tab_roster.html` (whole body, both scopes), `tab_draft.html` (the "Draft value & grades" charts + `standouts` + `finds` parts -- the round x slot pick BOARD stays, it is real pick data), `tab_transactions.html` (whole body), `tab_report.html` (`.reportpanel` body; `tab("report")` also skips `report_parts` entirely), `weekly_report.html` (`{% if unplayed %}`, `report_weekly` route returns early), `redraft_report.html` (`{% if no_games %}`, `report_redraft` returns early before `_draft_sim_ctx`). `season_report()` writes a short standalone page. `_draft_part_standouts`/`_draft_part_finds` return empty server-side too (defense against a direct URL hit). `tab("weekly")` passes `week=0` (the draft view) when the season has a board and nothing is scored, rather than an all-zero week 1 (`_resolve_week`'s `week=None` default is still `s.last_week`, never 0). NOT gated: `draft.draft_standouts`'s cross-season data, `metrics.record_book`, the History/Career tab (cross-season, has real data), and the Playoffs BRACKET graphic (see the skeleton-bracket bullet). Verified by direct route calls: DDBM + Coin Flip 2026 render notes only, 0 chart imgs, 0 warnings; every finished season is byte-for-byte unaffected. `report_parts` and all parity frames untouched. Python-webapp-only. **Two former cosmetic gaps here, both fixed 2026-09:** (1) `plots.plot_clutch` gained the empty-frame guard `plot_consolation_clutch` already had -- `if d.empty: return _no_data("No playoff games scored yet.")` right after the `_clutch(...)` call, before the `.sort_values("po_ppg")` that used to `KeyError` on an all-`PENDING` bracket. A direct `GET /chart/clutch` for an unstarted season now returns a titled blank panel instead of a 500 (still unreachable by clicking -- every in-app `clutch` / `consolation_clutch` request is inside `{% if po_started %}`). Regression-tested: `tests/test_playoffs.py::test_chart_clutch_no_500_on_unstarted_bracket`. (2) `_liveband.html`'s `{% if live %}` is now `{% if live and scored %}`, so the "`<year>` in progress: figures through week N" band no longer renders pre-season directly above the "no games scored yet" gate note. Both webapp-only; not mirrored to R.
- **A Sleeper chain only links BACKWARD, so a pasted older-season league id is resolved FORWARD to the current season (2026-09, `league.current_season_league_id` + `season.seasons(..., advance=)`).** `league_chain()` walks `previous_league_id` only, so pasting a 2024-season id for a still-running league capped the season selector at 2024. `current_season_league_id(league_id, nfl_season=None)`: short-circuits to the input if its chain already contains the current NFL season (`/state/nfl` `league_season`); otherwise pasted league's `/rosters` -> member `owner_id`s -> `sleeper_api_many` of `/user/{uid}/leagues/nfl/{season}` -> the first candidate whose own chain ROOT (`next(iter(league_chain(cand).values()))`, oldest-first) equals the pasted league's root. The ROOT is the identity key because two different leagues can share a name/season/most members. **Never raises**; ANY failure (or an already-current chain, or no member's current-season league sharing the root) returns the input unchanged -- a stale selector always beats a wrong league. Memoised 6h (`_forward_cache`, `clear_forward_cache()`, called by `refresh=1`). `season.seasons(id, advance=False)` / `season(..., advance=False)`: opt-in flag, **default OFF** so `parity/export_py.py` and `verify.py` (bare league id) are untouched -- ONLY `webapp.app.league_data()` opts in. It assembles from `resolved`, caches the entry under BOTH the submitted and resolved ids, and stores `d["resolved_league_id"]` / `d["submitted_league_id"]`; `tab()` + every page route after `pick()` reassign `league = d["resolved_league_id"]` so the pushed URL, header `#league` field (oob-swapped via `_shell_sync.html`) and every chart `<img>` src use the current-season id. `index.html`'s mode-toggle IIFE re-queries `#league` by id inside `syncUI()` every call, because an oob element swap detaches a captured reference. Python-webapp-only.
- **The skeleton playoff bracket + the condensed tie warning (2026-09, `playoffs.sleeper_bracket` + `playoffs.playoff`).** A season with no committed `season/<root>/<year>.json` falls back to Sleeper's own `winners_bracket` as the Playoffs-tab default (`playoff_source[year] == "sleeper"`, `league_data()`). Sleeper's PRE-season `winners_bracket` has round-1 nodes populated (`t1`/`t2` = roster ids) but every later round carries only `t1_from`/`t2_from` provenance (`{"w": 1}` = winner of matchup 1) with `t1`/`t2` null. `sleeper_bracket` now reads `m.get("t1") or m.get("t1_from")` (and `t2`); `_sleeper_side` already turns `{"w": m}` into `"W:M{m}"`, so the whole tree rebuilds (R2 = `W:M1 vs W:M2`, placement games, `final: M9`) and renders as "TBD" cards -- the "default path that fills in as results land". For a FINISHED bracket `t1` is a resolved id (truthy) so the `or` short-circuits: no behaviour change. The Playoffs tab renders the Bracket section in EVERY phase; everything else (KPI tiles, analytics, Results board, consolation panel) is wrapped in `{% if po_started %}` (`ctx["po_started"]` = any `p.results` row with a POSITIVE score, since Sleeper's pre-season bracket lists round-1 as decided-but-0-0). A **0.0-vs-0.0 matchup is `not_played`, not a tie** (a fantasy starter always accrues *something*): it emits PENDING rows (`points`/`opp_points`/`margin` = None), advances nobody, and stays silent, so the skeleton bracket's round-1 cards show `-` placeholders. A genuine non-zero tie still warns -- but `playoff()` now accumulates `tie_ids` + `lineup_problems` across the whole round loop and emits at most ONE `warnings.warn` each at the end (was one per matchup, which flooded the terminal on every Playoffs-tab load). `_gen_cache` in `playoffs.py` caches `sleeper_bracket` per `sleeper:<lid>:<season>` (`sm.playoffs._gen_cache.clear()` in tests). Python-webapp-only; R's `sl_playoff` / `sl_plot_playoff_bracket` unchanged.
- **The Weekly tab has a synthetic "Week 0" = Pre-season (the draft), `app._resolve_week` / `_week_context` `allow_zero=`, `tab_weekly.html` `preseason`.** The rail shows `Pre | 1 | 2 | ...`; `Pre` (`?week=0`) renders a pre-season view and skips every scored-week section (KPIs, narrative, `week_race`, scoreboard, best-of-week, moves). The view is a **collapsible per-member "Drafted Rosters" list** (`_preseason_rosters()` -> `_preseason_rosters.html`): one `<details class="dt-row">` per manager **in draft-slot (manager pick) order**, NOT regular-season finish. Summary columns are **Pick order** (the draft slot, first, left of Manager) / **Manager** / one **count per position** (`_PRESEASON_POS` = QB RB WR TE K DEF, plus an **Other** column only when a pick's position falls outside that list) -- how many of each the manager drafted. The drilldown lists that manager's picks in draft order as **Pick `round.pick` / Overall / ADP / +/- / Player / Team** -- position is appended to the player name (no rank, no its own column) and there are no season points, since it is pre-season. Two pick columns on purpose: **Pick** is the league's `round.pick` notation, **Overall** is the absolute `pick_no` -- and Overall is the one on the SAME SCALE as **ADP**, so Overall / ADP / +/- read straight across (the `2.10` vs `17.9` mismatch that made ADP "look wrong" was `round.pick` sitting next to an absolute number). **+/-** = `vs_adp = pick_no - adp`, tinted with the sign REVERSED: a NEGATIVE `+/-` (Overall < ADP, took the player earlier than consensus) is `.dv-pos` green, a POSITIVE `+/-` (later than consensus) is `.dv-neg` red. `team` (NFL abbr) comes from the cached player DB and `adp` (Sleeper's published pre-season ADP for the league's own format, via `draft._adp_field_for` + `draft._fetch_adp_raw`) from the season-scoped ADP snapshot; both are looked up in `_preseason_rosters` (NOT added to `draft_board()`, which is parity-adjacent) and degrade to "N/A" off-network. ADP and `pick_no` ARE the same scale (verified: `mean(pick_no - adp) = -0.89` over 170 real 2025 picks, no offset). Built straight off `draft.draft_board(s)` (no lineup/optimal solve); does **not** embed the Draft tab's board/standouts -- a plain "Draft Board" button in the header goes there for the value/grade charts, gem/bust reads and the redraft sim. The summary grid's `--cols` ends in a `1fr` **spacer** track (with a matching empty `<span>`) so the fixed position-count columns stay tight against the manager name instead of every column stretching on a wide panel; the drilldown `table.dt-games` is scoped `width:auto` under `.preseason-rost` in `style.css` (overriding the generic `#panel .dt-detail > table.dt-games { width:100% }`) so its six short columns don't spread out. Week 0 is offered **only when the season has a Sleeper draft board** (`not draft.draft_board(s).empty`, the same gate the Draft tab uses); `tab("weekly")` passes `allow_zero=has_draft` into `_week_context`, which then prepends `0` to `weeks` and returns all-null KPI tiles for it (skipping `week_stats` entirely). `_resolve_week`'s lower bound is `0 if allow_zero else 1`, so a stray `?week=0` on any other caller (the standalone `/report/weekly`, the Overview lead) still clamps up to week 1. `_week_context`'s default (`week=None`) is unchanged: it resolves to `s.last_week`, never 0.
- **Pre-season "Drafted Rosters" table layout: content-sized real columns, FLEXING spacer columns (2026-09, `_preseason_rosters.html` + `style.css` `.preseason-rost` block, ~16 iterative commits `c9c9c9c`..`4c6e386`).** The layout rule the user settled on, after several dead ends: the **column groups are fixed to their header/content width; empty spacer columns between the groups absorb all leftover width and scale with the window.** Two surfaces: (1) the **summary grid** (`.dt-head` / `.dt-row > summary`, `display:grid`, `grid-template-columns: var(--cols)`) is `Slot | Manager | ADP ± | QB RB WR TE K DEF [Other]` with `--cols: minmax(4rem,0.5fr) minmax(9rem,5fr) minmax(4.5rem,0.7fr) repeat(6or7, minmax(2.6rem,0.35fr))` -- Manager's `5fr` carries the slack so the grid fills the panel; `.pg1` on the QB column is a hairline `border-left`+`padding-left` separating ADP ± from the counts. (2) the **drilldown pick table** (`.dt-detail > table.dt-games`, `table-layout:auto`, `width:100%`, 8 cols `Pick | .gap-col | Overall | ADP | +/- | .gap-col.gap-wide | Player | Team`): real columns are content-sized via `width:1%` + `white-space:nowrap` under `table-layout:auto` (the tiny width loses to content+padding = shrink-to-fit); Player has `min-width:9rem`. The two `.gap-col` spacers take `width:40%` / `60%` (the `.gap-wide` second one wider -- Group 1 is `Pick` alone so its trailing gap needs less than the gap between the two dense blocks; two `width:100%` cols under `table-layout:auto` do NOT split evenly, the algorithm front-loads the first). Within-group gaps are `padding-LEFT: 38px` on `ADP`/`+/-`/`Team`, NOT padding-right -- on a right-aligned cell padding-right opens no gap to the next column, the gap comes from the LATER column's padding-left. `text-align` is set explicitly on BOTH `th` and `td` per column because the generic `table.dt-games th { text-align:left }` (0,1,1) outranks `.dt-num` (0,1,0). **The "ADP ±" summary column** (`app._preseason_rosters` `avg_vs_adp`) is the mean of `(pick_no - adp)` over a manager's priced picks (negative/green = drafted ahead of consensus), tinted like the per-pick `+/-`, `&ndash;` when None, and **nulled when `abs(avg_vs_adp) > len(board)`** -- a stub/partial draft (e.g. Coin Flip 2026, ~50 picks but ADP up to 700) has ADP on a different scale from its pick numbers. Rejected along the way: a `max-width` cap on `.preseason-rost` (then it didn't fill a maximized window); a trailing `1fr` spacer track in `--cols` (huge dead gap on the right); fixed-width (`1.8rem`) group separators (user wanted them to SCALE); `border-left` hairlines between drilldown groups (replaced with real empty `<td class="gap-col">`); Player `min-width:15rem` (padded the column with trailing whitespace). Python-webapp-only.
- **Playoffs tab renamed "Playoffs &amp; Consolation Bracket"; Closest-game KPI tile removed; clutch charts sort by postseason scoring.** The tab's `<h2>` and its KPI row (Champion / Consolation bracket champion / Biggest loser / Highest playoff score+round / Biggest blowout+round / Best postseason PPG) -- Closest game is gone.  Results-board `outcome`s for missed-bracket teams read "Lost in Round N (Consolation)", with `winner` -> "Consolation bracket champion" (forced first) and `last` -> "Biggest loser (Consolation)" (forced last).  `plot_clutch` / `plot_consolation_clutch` now sort rows by `po_ppg` / `to_ppg` (postseason scoring), not the `clutch` delta.
- **Roster tab: league scope and manager scope render the SAME summary-row columns (`_roster_by_manager.html`, `metrics._roster_detail_impl`).** Order is **Manager | Avg flex | Best PPG | Best player | Season pts | Players**; both scopes compute `best_ppg_p`/`best_pts_p` once, above the `{% if manager %}` branch, so a manager's row looks byte-identical whether reached by expanding it from the league table or by switching to that manager's own tab scope. **Best PPG**/**Best player** are PLAYER-level facts (`r.players | max(attribute="started_ppg"/"total_pts")`), not position-level; they used to be "which position scored fastest"/"which position got rostered most" via `pos_avg`/`pos_counts` dictsort, changed to player-level per follow-up request. Both render through the shared `player_chip(p)` macro (headshot + name + `POS #rank`, omitted cleanly when `pos_rank` is `None`, never prints "#None"). **Season pts is `r.started_pts` ONLY**: despite the label never changing, the underlying value flip-flopped mid-effort (started-only to started+bench to back to started-only); the hint blurb spells out "what they actually started, bench production isn't counted" precisely because the label alone no longer implies it. Jinja's `max` filter has no `default` kwarg (unlike Python's builtin): `{{ items | max(attribute="x", default=none) }}` raises `TypeError` rather than degrading gracefully, so an outer `if items else none` ternary is required, or any manager with zero rostered players 500s.
- **Roster drilldown table gets two new rate columns and per-role denominators (`_roster_macros.html::roster_players_table`, `metrics._roster_detail_impl`).** Column order: Player, Pos, Wks, PPG, Total pts, **Starting PPG**, Starter pts, **Bench PPG**, Bench pts. `started_ppg = started_pts / max(started_weeks, 1)` and `bench_ppg = bench_pts / max(bench_weeks, 1)`; `started_weeks`/`bench_weeks` are genuinely separate fields from the pre-existing combined `weeks`, added specifically so a player who mixed started/benched weeks in the same stint doesn't get an understated rate from the wrong denominator. The whole table lives in `.roster-scroll` (max-height 420px, sticky `<thead>`, pure CSS) at BOTH scopes; league scope's `<details>` drilldown gets scrollability but deliberately NOT the position click-filter (that's manager scope's own `.pblabel` chips, now `<button>`s instead of `<span>`s, wired to `data-filter-pos`/`data-pos` + `index.html`'s `prepRosterFilters()`; clicking the active chip clears the filter). `.roster-scroll`'s CSS picked up a second fix this session (`display:block; width:100%` on the div, plus a `.roster-scroll table.dt-games { width:100% }` rule) once `_draft_simulation.html` started nesting it one level deeper than its original `.dt-detail`-direct-child usage; a bare div with only `max-height`/`overflow-y` shrinks to its content's width outside that context.
- **`metrics.py` gained a small in-process cache (`_cached`/`clear_metrics_cache`) for the four Roster-tab functions that walk every manager × week (`roster_detail`/`roster_weeks`/`roster_honors`/`player_honors`).** Keyed on `f"{league_id}:{season}:{fn.__name__}"` (same convention `draft.py`'s own cache already uses, not `id(s)`, so a hit survives even a different `Season` instance for the same league+season) and returns a `copy.deepcopy` of the cached value, never the object itself: at least one caller (the Roster tab's own enrichment step) mutates rows in place, which would otherwise corrupt the cache for every other request sharing that `Season`. The webapp's `refresh=1` path calls `metrics.clear_metrics_cache()` (mirroring `scoring.clear_stats_cache()`) so a re-scored live week isn't served stale roster numbers for the rest of the TTL.
- **"Roster honors" (`metrics.roster_honors`/`player_honors`/`player_honors_by_position`) lost its Best-bench column and tally entirely (2026-08).** `best_benched_weeks` is removed from all three functions' output; the underlying per-week fact is still available as `roster_weeks()`'s own `best_benched`, just no longer summed/grouped anywhere the Roster tab reads. The three remaining honors (TOTW, Best lineup, Player of the Week) were relabeled "awards" in `_roster_honors.html` ("TOTW picks" to "TOTW awards", etc.), and each summary row now carries the player's `position` + season-final `pos_rank` (via `season_position_ranks()`, attached once per summary row) rendered as a `POS #rank` badge next to the name, previously bare name + headshot only.
- **Manager-scope "Roster standouts" tiles are a DIFFERENT tile set from league scope's, not a filtered version of it (`app.py::_manager_roster_standouts`).** League scope still shows `roster_standouts()` + `coaching_standouts()`'s league-wide superlatives (which lost several tiles this session too: "Best player-week", both bench-points tiles, "Most perfect lineups", "Losses the bench cost", "Worst single week" were all dropped from those two functions, not just re-homed). Manager scope instead shows: **Best value pick** (highest `ppg` among players THIS manager drafted, cross-referenced against `draft.draft_board()`, filtered to players rostered 3+ weeks; deliberately excludes trade/waiver acquisitions, and a league/season with no draft data silently omits the tile rather than showing a wrong fallback; this is the one place the Roster tab now pays draft.py's network cost), **Flex MVP** (`r.flex_top` + its share), and **Best/worst lineup week** (from `coaching_detail()`'s per-week `eff`). `tab_roster.html`'s standouts lazy-part is now unconditionally loaded (was league-scope-only before).
- **The Roster tab's per-manager view is now three sections: Roster, Roster breakdown (folded into Roster's own row), and a new Efficiency section, not four.** `_roster_fa.html` (the free-agent-comparison section, backed by `metrics.free_agent_best_team_season`) is DELETED, and `free_agent_best_team_season()` itself is removed from metrics.py; both `fa_season_optimal`'s `SEASON_CHARTS`/`CHART_META` entries and its `@tab_part("roster","fa")` handler are gone too. The new **Efficiency** section (`_roster_efficiency.html`, manager-scope only) is a per-week actual-vs-optimal lineup drilldown backed by a new `metrics.manager_week_lineups(s, manager)`, each row stamped with that WEEK's own position rank (`week_position_ranks()`, called once per week actually played, not the single-week-scoped `idm.posrank` context var the weekly report macro uses). Its lineup rows carry a new `swapped` flag (True on a started player the optimal lineup would drop, or an optimal pick that wasn't started) rendered with the same `.bench-impact` gold highlight the bench section's `would_start`/`was_started` flags already use, so a swap reads consistently across both the Started and Optimal tables. `bench`'s `would_start` here is deliberately NARROWER than `week_matchups()`'s own version of the same flag (any bench player individually beating the worst same-position starter): with the Optimal table sitting right beside it, that looser rule could gold-highlight a bench player the optimal lineup never actually picked (real case: LuckyHarm wk6 2025, two bench WRs each individually beat the starter but only one had an open slot); here it's `player_id in opt_ids`, true only for a bench player who IS one of the optimal lineup's actual picks. `week_matchups()`'s own `would_start` was tightened to the identical `opt_ids`-based rule for the same reason.
- **Roster tab league-scope chart placement reshuffled (`app.py::_roster_part_ctx`).** `position_scoring` ("Where the Points Come From") was removed from this tab's chart list only; the function/metric/`SEASON_CHARTS`/`CHART_META` entry are untouched, since `report.py`'s standalone season report and the webapp's own Season Report tab both still render it via `/chart/position_scoring` independently. `roster_counts` was promoted up to fill that slot; a new `plots.plot_season_optimal` (league-wide average Started-vs-Optimal by week, promoted out of the Testing tab) fills `roster_counts`'s old spot beside `efficiency`. Watch for `CHART_META["wide"]`: a chart promoted out of Testing (which defaults new prototypes to `wide: True` for visibility) silently breaks `.grid`'s side-by-side layout even at a normal figsize (`.grid .card.chart.wide { grid-column: 1 / -1 }` forces the full row); `season_optimal` had this flag and had to have it stripped before it would sit beside `efficiency` the way `mgr_roster_heatmap`+`mgr_optimal` already do.
- **Efficiency-axis floor for Started-vs-Optimal charts is centralized in one function, `_eff_axis_floor(eff)` (plots.py): `max(int(float(eff.min()) // 10 * 10), 0)`.** The floor is the lowest REAL value in whatever series it's handed, rounded down to the nearest 10, never a fixed constant (an earlier `min(50, ...)` was backwards: `min()` keeps the SMALLER value, so it forced the floor down to 50 even when the real low was higher, e.g. stranding a season whose worst week was 77% in the top ~40% of its own axis) and never average-derived (a season-mean-based version was tried and rejected the same way). Top is always a fixed `102` (not 100) so a real 100%-efficiency week's marker never sits flush on the axis edge; ticks via `MultipleLocator(10)`. **The two callers pass DIFFERENT series, and this is a deliberate divergence, not a bug**: `plot_season_optimal` (league-wide average, one line) stays self-scoped, its own per-week average series, floor independent of every other chart. `plot_mgr_optimal` (per-manager chart) instead passes the WHOLE LEAGUE's efficiency series (every manager, every week, `through_week`-scoped when capped) so every per-manager chart shares ONE floor and stays comparable side by side when flipped between managers; this supersedes an earlier state where both charts were purely self-scoped per-chart. Don't reintroduce a hardcoded floor or a duplicate formula in either caller; change the rule (or which series gets passed in) at the call site, never inside `_eff_axis_floor` itself.
- **`_highlight_eff_extremes(ax2, w, eff)` (plots.py) marks the best/worst week on a Started-vs-Optimal trend line, shared by `plot_mgr_optimal`/`plot_season_optimal`.** Lowest week always gets a labelled dot (blue, `#1f77b4`). Highest week gets a labelled dot too, UNLESS it's a real 100% (efficiency is capped there), in which case it draws a gold star (`#f1c40f`) with no value label, since "100%" is already stated by the axis top plus tick. If the best and worst week are the same single week, the low styling wins (a star on a lone below-100% week would misrepresent it as flawless). Colors are deliberately NOT the chart's own green/red bar palette; gold/blue read as a distinct layer instead of blending into the bars underneath; each marker has a thick white ring for contrast against whatever bar color it lands on, and each value label sits in a `T["bg"]`-colored rounded pill (same convention `_place_labels` uses) rather than bare colored text, since plain text in these colors was still hard to read against the green bar fill and grid lines. Skips silently under 2 points.
- **New chart `plot_playoff_players_splice` is now the PRODUCTION "Best Playoff Players" chart on the Playoffs tab (replacing `plot_playoff_players` there; see the corrected scope-control bullet above).** Instead of one solid bar per player, each player's total-points bar is cut into one SEGMENT PER PLAYOFF GAME (reading `playoff_performances()`, the row-per-player-week grain `playoff_players()` itself aggregates from), ordered chronologically (earliest career playoff game first, left to right) so a late hot streak or an early quiet stretch shows as a shape, not just an implied total; since this is a CAREER leaderboard, a player's games can span multiple seasons. Color is GAME SLOT (1st game, 2nd, 3rd…), not position; it reuses `_MANAGER_HUES` purely as an ordered categorical set keyed by slot index, the same "index by position in a sequence, not by manager identity" trick `plot_trade_single_contribution` already established for its own per-player segments (see that function for the precedent); position moves into the row's text label instead, and there's no swatch legend since a "game 1/2/3…" key would need as many entries as the deepest career run. The label also carries a `POS #rank` badge; since this is a career chart, a player can have scored in more than one season, so "the" rank is ambiguous; it resolves to that player's MOST RECENT scoring season's position rank, which is why this function (and its caller in app.py) now takes a `seasons: dict` param (`{season_str: Season}`, the same shape `plot_clutch` already takes); a season key with no matching `Season`, or no rank for that player, degrades to no badge rather than failing the chart. The Testing tab still carries THREE surviving prototypes of the ORIGINAL non-spliced chart under `TESTING_CHARTS`: `playoff_players_topband` (legend moved above the axes), `playoff_players_postext` (position as text, no legend), `playoff_players_medal` (ring count as medal dots instead of trailing stars); none promoted or rejected yet; the four `mgr_position_stack`/`mgr_position_stack_pct`/`position_scoring_range`/`position_scoring_by_mgr` "Where the Points Come From" alternatives from an earlier round are still there too (also undecided), while the still-earlier "Points per Roster Slot" round (9 chart functions plus their shared `metrics.position_value_density()` support function) was removed from `TESTING_CHARTS` entirely and fully deleted, not merely hidden.
- **Playoff bracket-round lineups now show headshots by default (`_playoff_round.html`, `_lineupmacro.html`).** `show_id` used to be passed as `false` for this one caller; the playoff engine's submitted lineups predated carrying `player_id`, so there was nothing to key a headshot off of. `player_id` has since been added to those rows (feeding the existing "POS #rank" badge, which already worked regardless of `show_id`), so the `show_id=false` override was simply stale and has been dropped; this caller now gets the same default (`show_id=True`) as every other `lineup_table()` caller.
- **Working directory.** `readRDS`/`ggsave` use bare filenames, so the R working directory must be the repo root (RStudio sets this from the `.Rproj`).
- The `.RData*`, `.Rhistory*`, and `.Rproj.user/` files are RStudio session state, not source.
